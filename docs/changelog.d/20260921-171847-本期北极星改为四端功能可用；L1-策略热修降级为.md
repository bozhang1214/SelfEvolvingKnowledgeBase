## 2026-09-21（本期北极星 = 四端功能可用；L1 策略热修本期实现，设备适配子集只预留（D18））

- **背景**：owner 先指出"Android 机型/ROM 复杂，L1 可以适当考虑但不作本期重点；本期重点是这几个端的功能可用"，
  随后明确口径：**L1 正常实现**，**只有"Android 设备适配相关"的策略/配置预留、本期不实现**，其余按建议开展。

- **改动**（**只改文档口径，未动代码**）：
  - `docs/RFC-多端跨端方案.md`：§0 前加 **🎯 本期北极星**；
    §2.1 目标按优先级重排（功能可用第一）；**新增 §2.3「功能可用」验收清单 F1–F10（四端逐项对等）**；
    §7 新增 **M4.5 里程碑 = L1 策略热修（本期实现，2–3 天）**及其四条可证伪验收；
    §9 增 **Q7 决策记录**（L1 正常实现 / 设备适配子集预留）。
  - `docs/多端跨端-工程议题（网络层·包体·热修复）.md`：§3.2 改写为 **L1 本期实现的规格**——
    可变项白名单表（8 项）、`GET /edge/policy` 响应字段（version/issuedAt/expiresAt/embeddingSpace/
    rollout/signature）、签名、灰度分桶、双槽回滚、审计日志，以及端侧**应用前三道校验**
    （签名 / 空间戳一致 / 键必须在白名单内，多一个键也整包拒绝）与四条验收；
    **新增 §3.3 预留：设备适配类策略**（给出占位 schema `deviceProfiles: {}` 与"不读取、白名单跳过"的落地方式，
    并写明为什么现在不做：没有真机矩阵，写了也是猜）；§3.4 L2 只做一半（iOS 首版内嵌 int8 模型保功能可用，
    下载通道后置）；§4 汇总表与 S4 同步。
  - `docs/BACKLOG.md`：**D18 改为「设备适配类策略只预留」**（含触发条件），L1 其余部分转为本期实现（M4.5）。

- **验证**：`doc_guard.sh` 6/6 全绿。本轮无新增外部断言（沿用已实测事实），
  新增的都是**待实现的验收标准**，不是已达成的结论。

- **下一步（按 §5.2 开工 M4）**：① 契约夹具先行 + Android runner；② 依赖去平台化；
  ③ R10（`DEVICE_ONLY` 本机判据）收口；④ P0 包体瘦身；⑤ M4.5 L1 策略热修（服务端端点 + 端侧应用）。

## 2026-09-21（M6 spike：第 2 条已验证 + 第 1 条配方拿全 + 第 3/4 条外部阻塞）

- **背景**：上轮把前置条件查清，本轮把能验的验掉、把配方拿全。

- **第 2 条（kotlinx 库在 ohos 上可解析）→ ✅ 产物层面已验证**：
  第三方 nexus（`maven.eazytec-cloud.com/nexus/repository/maven-public/`）上
  `kotlinx-coroutines-core-ohosarm64:1.10.2-1.0.0` 与
  `kotlinx-serialization-json-ohosarm64:1.9.1-1.0.0` 的 klib **都是 HTTP 200**。
  （"依赖供给"是这条路线的最大风险，现在从"担心"变成"已验证"。）

- **第 1 条（KMP 产物 + HAP 经 NAPI 调用）→ 🟡 配方已拿全**：clone 官方 KMP sample
  （`gitcode.com/CPF-KMP-CMP/kmp-cmp-example`，`kmp-example` 分支）并抄出关键配置：
  Kotlin fork **`2.2.21-0.3.0-07`**、`ohosArm64`/`ohosX64` 两个 target、
  `maven.eazytec-cloud.com/nexus/repository/maven-public/` 单一仓库、
  `kotlin.mpp.applyDefaultHierarchyTemplate=false` + 手工 `ohosMain` 源集、
  `kotlin.native.cacheKind.ohosArm64=none`（与我在 iOS 上撞到的 klib cache 同源问题）。
  **注意**：sample 用 AGP 8.11.2，而我们现有 Android 构建是 AGP 9 →
  spike 要用**独立 Gradle 构建**，不能搅乱已验证的 Android 基线。

- **第 3 条（ONNX Runtime 自建）→ ❌ 网络阻塞**：ORT 源码在 GitHub（实测 HTTP 000），需找镜像。

- **第 4 条（HAP 能装能起）→ ⛔ 需 owner 的华为开发者账号**（DevEco 模拟器需登录、应用必须签名）。

- **下一步**：用独立构建试第 1 条（编 `libkn.so` + 最小 HAP 经 NAPI 调用）；
  同时找 ORT 的 OHOS 镜像。第 4 条等 owner。四条全通才进 M7，否则退 ArkTS + 契约夹具。

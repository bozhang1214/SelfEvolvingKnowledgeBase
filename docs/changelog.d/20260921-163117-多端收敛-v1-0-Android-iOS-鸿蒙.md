## 2026-09-21（多端收敛 v1.0：Android/iOS/鸿蒙/Mac 四端定案，桌面搁置（D16））

- **背景**：owner 给出两条决定——**桌面端搁置**（计入跟踪项）、要求把 **Android / iOS / 鸿蒙 / Mac**
  四个端的建议收敛成一份可确认的方案。此前四份文档（总方案 v0.1 + KMP 详设 + CMP 评估 + 桌面审计）
  结论分散且 v0.1 还推荐 CMP，与 owner 的原生 UI 倾向冲突。

- **改动**（**只改文档，未动代码**）：
  - `docs/RFC-多端跨端方案.md` **重写为 v1.0 收敛版**（唯一决策入口）：§0 四端收敛表（UI/共享逻辑/
    端侧推理/顺序/工作量/本机可验证项）；§4 D1 **已定：UI 各端原生**（Compose/SwiftUI/ArkUI，
    Mac 复用 iOS 的 SwiftUI）；D2 鸿蒙 = KMP 逻辑 + ArkUI 原生 UI + **3 天 spike 四条验收**（不过则退
    ArkTS + 契约夹具）；D3 **桌面搁置**（价值=算力宿主、宿主人日 4.5–6.5、拓扑 A≠B、GUI 不做、
    重启用条件）；D4 四端运行时矩阵；§7 里程碑（M4 共享层 → M5 iOS → M6 spike/M7 鸿蒙 → M8 Mac）；
    §8 新增 **R10**（`DEVICE_ONLY` 在 LLM 平面缺"本机"判据）；§9 收敛为 **6 条待确认**。
  - `docs/BACKLOG.md`：新增 **D16 桌面端搁置**（含审计全部结论与重启用条件）、
    **D17 `DEVICE_ONLY` 语义收口**（嵌入侧已有 `isOnDevice` 闸门，LLM 侧只按 `edgeBaseUrl` 寻址——
    模拟器今天就指向 `10.0.2.2` 开发机，语义已不严谨；建议 M4 内 0.5–1 天收口）。
  - `apps/README.md` / `apps/ios/README.md` / `apps/harmony/README.md`：按收敛结论更新
    （原生 UI 定案、`mac/` 目录、桌面搁置、鸿蒙目标形态 = ArkUI + KMP 逻辑）。

- **验证**：`doc_guard.sh` 6/6 全绿（本次改动后重跑；链接/引用/新鲜度/活数字/协议三方/生成物同步）。
  本轮**未新增任何外部未核验断言**：收敛表里的数字全部沿用已实测项（70% 可移植、158/182 可共享、
  微基准 1.135/0.923/9.040 μs、模拟器自检 26/26、检索 87%/100%/0.928、OHOS NDK 编译实测），
  外部事实仍保留 §10 的 ✅/📄/⏳ 状态标注。

- **待办**：§9 六条待 owner 确认（范围锁定、原生 UI、顺序、鸿蒙 spike、R10 是否 M4 内做、是否允许改 CI）。
  确认后即可从 **M4 共享层**开工。

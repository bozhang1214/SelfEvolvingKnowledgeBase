## 2026-09-20（T1 端点表改为生成物：83 个端点与代码同步，文档守卫加第 6 项）

- **背景**：T1（路由事实表）是 2026-09-09 逆向分析期手抄的，2026-09-16 复核时已漂移
  （表里 65 条 vs 实测 75 个端点）。手抄的表**必然再次漂移**——`file:line` 和计数都靠人记，
  而代码每天都在加端点。端云协同 S1/S2 又新增了 device 5 / edge 3，共 8 个端点没人补。

- **改动**：
  - 新增 `scripts/gen_facts_routes.py`：正则解析 `APIRouter(prefix=…)` + `@router.<method>(…)`，
    按模块（15 个）生成方法/路径/端点级认证/处理函数/`file:line` 表格；
    `--write` 写回 T1 的 `BEGIN/END GENERATED` 标记之间，`--check` 只校验不写。
  - `docs/tech/.facts/T1-routes.md`：§1 起改为**生成物**；§0（鉴权模型、三类凭证、
    依赖口径、协议约定）仍人工维护——"机械可得"的生成，"需要理解的"留给人写。
  - `doc_guard.sh` 加第 6 项检查（生成物与代码同步），并把 `ROOT_DIR` 统一，守护从 5 项变 6 项。
  - `gen_facts_routes.py --check` 额外校验**下游数字**：`05-API-REFERENCE.md` 的
    `fact:api_endpoints` 必须等于代码实测值（同一个事实不允许两处取值）。

- **验证**：
  - `--check` → ✅ 83 个端点，且 05 数字一致；`doc_guard.sh` 6/6 全绿。
  - **反向验证（可证伪）**：把 05 的数字改成 82 → 立刻红（"下游数字不一致"）；
    破坏 T1 标记 → 立刻红（"缺少生成区块标记"）；还原后回绿。
  - 计数修正：原 `--check` 打印的"98 个端点"其实是**表格行数**（含表头分隔行），
    已改为由生成函数直接返回端点总数（83）——计数错误会让人误判，必须修。

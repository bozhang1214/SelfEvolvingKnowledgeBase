## 2026-09-21（多端跨端三份设计文档（KMP / CMP 评估 / 桌面审计）+ 微基准实测）

- **背景**：owner 对跨端方案给出反馈——**UI 倾向各端原生**（要求先看 CMP 的设计与接口）、
  要一版 KMP 方案确认、桌面要一份审计文档（并确认桌面=Windows + Ubuntu）；同时同意 Kotlin 升级。
  另外本轮把 owner 的两个疑问（"CMP 会影响原生体验吗""KMP 会不会损耗性能"）从"讲道理"变成"给证据"。

- **改动**（**只写设计文档，未动任何代码/构建**）：
  - `docs/多端跨端-KMP方案.md`（推荐主线）：KMP 逻辑共享 + 各端**原生 UI**；
    共享边界表、KMP 工程布局、**端口接口签名**（`HttpTransport`/`EmbeddingProvider`/`VectorStore`/
    `DocumentRegistry`/`CredentialStore` 已有 + `FileSourcePort`/`TextExtractPort`/`DeviceToolPort`/`PlatformEnv`/`EdgeConfig` 新增）、
    各端实现矩阵、端口设计的 6 条约束（粗粒度、流式不跨语言边界、不可变数据…）、
    性能与体积 4 项验收（P1–P4）、迁移 8 步、一致性机制、风险、工作量。
    **实测**：测试可共享 **158/182（87%）**；main 5,406 行里 **3,786 行（70%）可移植**。
  - `docs/多端跨端-CMP方案评估.md`：正面回答"原生体验"问题——16 项能力逐条对照（带官方链接与核验状态）、
    **12 条待本地验证项（V-1…V-12）各带方法与判定门槛**、CMP 设计 v1 与完整端口接口签名、
    值得/不值得的条件、回退触发，以及"若选原生，这份文档里 8 项设计仍然有用"。
  - `docs/多端跨端-桌面端方案审计.md`：对「headless 宿主 + CMP GUI」的审计。关键修正与发现：
    ① **拓扑 A≠B**——M0 的 profile 里 `planes.edge.base_url` 是 `127.0.0.1`，对手机毫无用处，
    所以"宿主"不是"复用 M0 脚本"，首版人日从 2–3 **修正为 4.5–6.5**；
    ② **`DEVICE_ONLY` 契约漏洞 F-1**：一旦"端侧"可以是局域网里的另一台机器，隐私硬边界被绕开
    → `DEVICE_ONLY` 必须显式定义为"本设备上的本地运行时"，宿主收到即拒 + 留痕，代手机推理不得落盘正文；
    ③ Ollama 的 OpenAI 兼容端点**无 API key** → 首版用"默认只绑 127.0.0.1 + Tailscale"网络隔离；
    ④ CMP 桌面**不支持交叉编译**（.msi 只能在 Windows 打、.deb 只能在 Linux 打），而本机只有 macOS
    → GUI 后置，先交"宿主 + 浏览器入口"。
  - `docs/RFC-多端跨端方案.md`：记录 owner 反馈（原生 UI 倾向、Kotlin 升级同意、三份文档入口）；
    新增风险 **R9**（F-1 契约漏洞，标注"排在桌面宿主功能之前做"）；E8 记录 OHOS NDK 交叉编译**已实测**。
  - `apps/harmony/README.md`：补 OHOS NDK 实测结果（clang 15.0.4 出 `aarch64` musl ELF；
    顺带记录"clang 15 基线偏老，ONNX Runtime 可能要吃补丁"这一 spike 风险）。
  - `docs/README.md`：索引加三行。

- **验证**：
  - **微基准（JVM，2026-09-21 实跑）**：`PlaneRouter.decide` **1.135 μs/次**、`ToolCallJson.parse` **0.923 μs/次**、
    `Chunking.split(2KB)` **9.040 μs/次** → 一次聊天十几微秒，相对端到端 **119ms** 约 **0.008%**，
    即"共享逻辑换语言实现"不可能成为延迟瓶颈（Kotlin/Native 版本仍待测，已列为 P1）。
    跑基准时顺带复跑全量：**182 个用例全部通过**（临时基准文件跑完已删，工作区无残留）。
  - **OHOS NDK 交叉编译实测**：`aarch64-unknown-linux-ohos-clang` 编 + 链出
    `ELF 64-bit LSB pie executable, ARM aarch64`（解释器 `/lib/ld-musl-aarch64.so.1`）→ "鸿蒙编 C/C++ 库"链路已通。
  - `doc_guard.sh` **6/6 全绿**（新增 3 个文件后：101 个 Markdown、0 错误）。

- **结论与待办**：三份文档**待 owner 审阅**。建议主线取 **KMP 共享逻辑 + 各端原生 UI**
  （与 owner 倾向一致，且 §7 一致性机制把"逻辑漂移"变成机器可判）；CMP 作为已评估的备选保留。

## 2026-09-21（M6 spike 前置调研：四个硬事实与一个 owner 前置条件）

- **背景**：M6 是"3 天 spike：四条验收，全通才进 M7"。开工前先把"能不能做"查清，
  而不是先写代码再撞墙。

- **做了什么**：把官方文档 clone 到 `.tooling/ohos-spike/docs`
  （`git clone --depth 1 https://atomgit.com/CPF-KMP-CMP/docs.git`，**AtomGit 可达**），
  逐条核对 spike 四条验收的前置条件，结论写进 `apps/harmony/README.md`。

- **四个硬事实（有文档依据）**：
  1. **工具链是 IDE 插件**（"KMP OHOS Support-1.0.0.zip"，离线安装；源为第三方 nexus，
     实测 **HTTP 200 可达**）→ 官方路径是 IDE，**纯命令行路径文档未承诺**；
  2. **必须签名才能装到真机/模拟器**，签名在 DevEco Studio 完成、要**华为开发者账号**
     → **spike 第 4 条需要 owner 的前置条件**；
  3. `ohosArm64` 是真机目标，**模拟器/x86_64 需额外启用 `ohosX64`**（本机模拟器是 x86_64）；
  4. 版本门槛：JDK17+/Gradle8+/DevEco **6.0.0+**/HarmonyOS SDK **API 17+** —— 本机均满足
     （SDK API 22、OHOS NDK、hvigor/llvm/node 齐备）。

- **另一处网络现实**：spike 第 3 条要自建 ONNX Runtime，而 ORT 源码在 **GitHub（实测 HTTP 000 不可达）**；
  可能的绕行是 AtomGit/GitCode 上的 OHOS 移植镜像，**待验证**。

- **下一步**：spike 第 1/2 条先试（KMP 产物 + kotlinx 在 ohos 上可解析）；
  第 3 条先找 ORT 源码镜像；第 4 条等 owner 的华为开发者账号。
  按 §4 D2：四条全通才进 M7，任一不过就退路线 B（ArkTS 重写 + 契约夹具）。

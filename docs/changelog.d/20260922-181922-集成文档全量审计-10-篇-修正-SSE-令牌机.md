## 2026-09-22（集成文档全量审计（10 篇）：修正 SSE 令牌机制误判 + 21 条待办）

- **背景**：owner 要求"跟着文档做就能完成对接"。对 `jobcopilot/docs/integrations/` 下
  **10 篇**文档做了逐篇审计（10 个并行 subagent，各自对照仓库代码核实，只报"读者会怎么失败"）。

- **本轮最重要的发现：审计推翻了我自己上一轮的结论。**
  我上轮写"千帆无法 BYOK，但仍能用 `self_check` / `list_prompt_packs`"——**错了**。
  机制已亲自核实（`mcp/client/sse.py:84`）：SSE 传输先发 `endpoint` 事件宣告消息端点，
  客户端用 `endpoint_url = urljoin(配置的 url, endpoint 事件内容)` 推导消息地址；
  服务端宣告的是 `/jobcopilot/sse/messages/?session_id=...`（**不带令牌**），
  而 `urljoin` 会用它的 query **覆盖**配置 URL 上的 `?token=`。
  ⇒ `POST /sse/messages/` 到了门禁是**没有令牌**的 → 401 `缺少访问令牌` →
  **连 `initialize` 都完不成**，`self_check` 同样到不了。
  `?token=` 只在 **`POST /mcp`（Streamable HTTP）**上有效（客户端把整个 URL 直接 POST）。
  - 已修正 `qianfan.md`（改成"当前根本建不起会话"+ 三条出路）与 `integrations/README.md`
    （千帆行改为"公网端点当前不可用"；索引表第 2 条那个 ✅ 改为"⚠️ 只对 `/mcp` 有效"）。
  - **教训**：`?token=` 兜底是**按传输分叉**的，不是"全局都行"。上一轮我只验证了
    `/mcp` 的三种 401/400 就下了结论，没有验证 SSE 的第二跳。

- **审计结论（10 篇）**：blocking 5 篇 / minor 4 篇 / 1 个 agent 失败（qianfan）。
  已在本轮修掉的：上面那两条 + 上一轮的 URL 前缀与鉴权头修正。

- **待办（已核实、尚未修，按严重度）**：
  1. **`save_profile` 在公网端点必然失败**（容器 `read_only`），而 `coze.md`/`dify.md`
     把它列出、`server.py` 的 INSTRUCTIONS 还让客户端"先 save_profile"→ 读者拿到
     "内部错误"，且后续 `analyze_job` 静默以空画像跑、无任何线索。
     **5 篇平台文档都缺**"改用按请求传 `user_profile` 参数"这条。（blocking）
  2. **`bailian.md` 漏了"把 MCP 服务绑到智能体/工作流"这一步** → 读者停在"服务状态可用"，
     回来选不到工具，误判成鉴权/证书问题。（blocking）
  3. **`dify.md` §2 配置表仍写 `Authorization: Bearer <Key 或令牌>`**，
     与同节正确段落自相矛盾，且"自定义请求头"行没有 `X-JobCopilot-Api-Key`。（blocking）
  4. **`DEPLOY-HTTP.md` §3 标题说覆盖 SSE，nginx 配方却只暴露 `/mcp`**，没有 `/sse`
     与 `/sse/messages/` → 照做后千帆这条链路必不通。（blocking）
  5. **`dsh.md` 两处误导**：§4 写"列得出 **6** 个才算接上"（实际 **7** 个，
     同篇 §3 自己写 7，`7a6f8a7` 的 6→7 漏改了这句）；§2 用
     `!!js process.env.JOBCOPILOT_LLM_API_KEY` 却全篇没说要**先导出它**——
     未导出时是 schemastery 校验失败、该条目**根本不加载**（0 个工具），
     恰好制造了 `sekb.md` 宣称已消除的那个症状。（misleading）
  6. **`sekb.md` §3.1** 把 `jobcopilot doctor` 与 `/health` 并列当同一判据，但两者不同源：
     `doctor` 读宿主进程环境（该部署下没有 `JOBCOPILOT_PROMPTS_DIR`）→ 误报 `base`，
     而 `/health` 与子进程同源、显示 `local`。读者会去修一个不存在的故障。（misleading）
  7. **`PRIVACY.md` §2.3 把 `JOBCOPILOT_ALLOW_SOURCE_PATH=1` 写成"确需启用"的可选项**，
     与运维文档"**绝不**给公网容器配"口径冲突（公网开=任意文件读取）。（misleading）
  8. **可验证性普遍缺失**：`bailian`/`dify`/`hiagent`/`coze` 都**没有一条命令能自证成功**，
     而内核专为此提供了 `self_check`（固定 <1KB），几乎没被提到。（minor→实际影响排障）
  9. **代码级真 bug（不是文档问题）**：`server.py` 的 `analyze_job` 把 `max_chars`
     声明进了 schema 却没透传给 `guard(...)`（`analyze_jobs_batch` 传了）→
     云端平台按提示填了长度约束**静默无效**。建议补传 + 加单测。（建议单独立项）
  10. 其余 minor：`coze.md`/`bailian.md` 声称支持 SSE 却不给 `/sse` 地址；
      `hiagent.md` 全文**零鉴权信息**（0 次出现 Bearer/令牌/401）且端点只有裸 URL；
      `dify.md` 缺"注册后还要挂到应用/节点"这一步；`bailian.md` 的 `type` 字段与 `coze.md` 不一致。

- **验证**：`check_kernel.sh` 改前确认无人在动内核；`doc_guard` 通过；
  子模块改动推送后 SEKB 指针同步更新于同一提交。

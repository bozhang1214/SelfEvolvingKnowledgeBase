## 2026-09-21（M4.5 E2E 跑通：自检 28 PASS / 0 FAIL / 2 SKIP）

- **背景**：上一轮 L1 接线完成后 E2E 没跑通（自检第 1 项 `edge_reachable=false`）。本轮把它推到绿。

- **改动**：
  - `scripts/android.sh` 支持 `SEKB_EDGE_URL=`（构建期覆盖端侧端点，对称于已有的 `SEKB_SEKB_URL`），
    便于走 `adb reverse` 路径（`http://127.0.0.1:11434/v1`）；
  - `scripts/emulator.sh` 起好模拟器后自动 `adb reverse tcp:11434 tcp:11434`（上一轮已加，本轮实测生效）；
  - 自检调整：`policy_rollback_guard` 移出"有凭证才执行"的分支——它与凭证无关，
    无上一份时必须干净跳过，应该始终出现在自检结果里。

- **验证（真实输出）**：
  ```
  [PASS] edge_reachable — http://10.0.2.2:11434/v1 → true
  [SKIP] policy_refresh — 无设备凭证（先登录换设备 token）→ 跳过
  [PASS] policy_rollback_guard — 无上一份时回滚必须干净跳过（不抛、不改状态）
  自检汇总：PASS=28 FAIL=0 SKIP=2
  ```
  两个 SKIP 都是"缺前置条件"而非失败：`cloud_login`（未提供账号）与 `policy_refresh`
  （策略端点按设备 token 鉴权，未登录）——拿不到策略时端侧照常工作，`PolicyFetcherTest` 已钉住
  "任何失败都不改状态"。全仓文档里的 `27/27` 已同步为 `28 PASS / 0 FAIL / 2 SKIP`。
  `bash scripts/android.sh test` → 231 测试全绿；`doc_guard` 6/6。

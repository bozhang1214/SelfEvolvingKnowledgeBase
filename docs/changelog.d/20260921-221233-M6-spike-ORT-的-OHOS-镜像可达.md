## 2026-09-21（M6 spike：ORT 的 OHOS 镜像可达 + hvigor/ohpm CLI 可用）

- **背景**：上轮结论是"第 3 条被 GitHub 封锁、第 4 条需 owner 账号"。本轮把这两条外部路径各自确认一次。

- **实测**：
  | 路径 | 结果 |
  |---|---|
  | `code.ruyicommunity.cn/xw1216/onnxruntime`（社区 OHOS 移植，带 `docs/ohos/build_deploy_usage.md`） | **HTTP 200**，已 `git clone --depth 1` 到 `.tooling/ohos-spike/ort-ohos` |
  | `gitcode.com/OpenHarmony-AI-Components/ohos_model_benchmark` | **HTTP 200**（旁证生态存在） |
  | `gitcode.com/openharmony/third_party_onnxruntime` | 404（路径不对） |
  | GitHub 官方 ONNX Runtime | HTTP 000（不可达，已排除） |
  | `hvigorw` / `ohpm`（DevEco 自带 CLI） | ✅ 都在 `/Applications/DevEco-Studio.app/Contents/tools/` |

- **结论**：
  1. 第 3 条的**源码获取问题已解决**（镜像可达），剩下的是"实际编译 libonnxruntime.so"——
     体积大、耗时长，属独立一轮；
  2. 第 1 条的 **HAP 半段可以本地做**（hvigor CLI 可编未签名 HAP），
     **只有"装到设备/模拟器"需要 owner 的华为开发者账号**（官方：鸿蒙应用必须签名才能安装）。

- **下一步**：① 编 ORT 的 OHOS arm64（或先只做可行性验证：configure + 编译一小部分）；
  ② 写 HAP 壳工程 + NAPI 桥（本地编出未签名 HAP，把 libkn.so 打进去）。

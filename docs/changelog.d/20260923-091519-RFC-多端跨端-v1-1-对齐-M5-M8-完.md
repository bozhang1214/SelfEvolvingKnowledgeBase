## 2026-09-23（RFC 多端跨端 v1.1：对齐 M5/M8 完成与鸿蒙运行时订正（含过期数字修订））

- **背景**：这套多端程序的"记录在案"文件是 `docs/RFC-多端跨端方案.md`。经过 M5/M6/M8 这几轮实测，
  它的多处关键结论**已经被推翻**（鸿蒙选"自建 ONNX Runtime"、Mac 沿用 Android 的 JVM ORT、
  若干验收基线数字过期），而真机调试要顺延到次日——所以本轮做的是**让 RFC 与实测一致**，
  而不是继续推阻塞项。

- **改动**（`docs/RFC-多端跨端方案.md`，74 增 / 32 删；版本 v1.0.1 → **v1.1.0**）：
  - **§0 四端收敛表**：Android/iOS/Mac 标为已完成并写入实测自检数（26 PASS/0 FAIL/1 SKIP）；
    鸿蒙一行改为"MindSpore Lite + 三条待真机"。
  - **§1 硬事实**：`shared/commonMain` 26 文件/3,395 行 → **35 文件/4,066 行**；单测 207 → **231**；
    契约夹具写明 **37 个 case**（routing 10 + signals 14 + privacy 13）。
  - **§2.3**：补"三端已同表"的实测注（五个阈值 Hit@1/Hit@3/MRR/误召回逐项相同）。
  - **§4 D2**：spike 四条验收改成**逐条对照表**（原文 / 实际 / 证据），并写明
    "✅ 仅指产物与链接层面"的口径纪律——避免被读成"已经跑通"。
  - **§4 D4**：新增"运行时矩阵的两处订正"——① 鸿蒙改系统 MindSpore Lite（原自建 ORT 作废，含证据）；
    ② **Mac 不沿用 JVM ORT，而是复用 iOS 的 Swift+C API 实现**（原文是未经验证的假设；
    这也解释了 M8 为何从原估 4–6 天变成 ≈1 天）。
  - **§7 里程碑**：M4/M4.5 ✅、**M5 ✅（实际 ≈2 天 vs 原估 12–16 天）**、M6 🟡（未收口，且**尚未**触发退路线 B）、
    M7 ⏸、**M8 ✅（≈1 天 vs 原估 4–6 天）**。
  - **§9 决策记录**：新增 Q8（鸿蒙运行时改 MindSpore Lite，owner 已确认）、Q9（路线① 上限 1–2 轮，
    owner 已确认）、Q10（真机调试进行中、顺延次日）；开工状态更新为"当前唯一阻塞 = 真机 + 签名材料"。
  - **§10 外部事实**：E5 标作废；E8/E9 由 ⏳ 改 ✅；**新增 E11**（模型转换链：converter_lite 无 macOS 版 →
    用 Linux-aarch64 包在本地 arm64 容器原生跑；四步图重写；`.ms` 已产出；Linux benchmark 卡死未通过）、
    **E12**（ORT 的 iOS/macOS 产物来源**不在 GitHub**，在 download.onnxruntime.ai；含"先确认唯一来源"的教训）、
    **E13**（签名与设备现状实测：账号已登录且实名认证完成，但无签名材料、无模拟器镜像、hdc/adb 都看不到手机）。
  - **过期基线修订**：把"将来要复跑"的门禁口径改成当前值（§5.2 步 5 与 R1 的 207 → **231**），
    历史步骤保留当轮数字但标注"现已增至"；M4.5 的后端基线标注为"当轮 971 / 当前实测 **994**"。

- **验证**：
  - `bash docs/tech/.validation/doc_guard.sh` → **6/6 全通过**（HEAD=fe2a1c0）。
  - 自写脚本校验**所有 Markdown 表格列数一致**（改表格最容易漏掉一列）→ 无异常。
  - 复跑被引用的数字：Android **231 用例 / 0 失败 / 0 跳过**、后端 **994 passed** —— 与 RFC 写入值一致。
  - `grep '\b207\b'` 复查无残留。
  - 本轮**只改文档**，未触及代码/共享层，故 Android/后端套件结论不受影响（仍按 DoD 复跑确认为准）。

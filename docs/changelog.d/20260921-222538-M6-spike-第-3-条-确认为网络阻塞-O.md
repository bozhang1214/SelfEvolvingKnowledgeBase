## 2026-09-21（M6 spike 第 3 条：确认为网络阻塞——ORT 外部依赖全在 GitHub）

- **背景**：上轮把 ORT 的 OHOS aarch64 编译启动（参数、工具链文件、python、cmake 都纠正过）。
  本轮取结果：**没有任何产物**——`build/ohos_aarch64` 始终 0B、`.o` 数为 0、25 分钟无进展。

- **查证结论：卡在 `--update` 拉外部依赖，而它们全在 github.com（本机不可达）**：
  | 证据 | 内容 |
  |---|---|
  | `ort-ohos/.gitmodules` | 三个子模块 URL 全指向 github.com（onnx/onnx、libprotobuf-mutator、emsdk） |
  | `cmake/external/` | 只有 `.cmake` 脚本，没有源码（abseil/protobuf/onnx 等需现拉） |
  | 网络 | github.com **HTTP 000**（与 M5 端口③ ORT iOS 是同一堵墙） |
  | 镜像仓库 | 不含预编译 `libonnxruntime.so` |

- **三条可行路径（都需要外部条件）**：
  1. 在能访问 GitHub 的机器上编译 OHOS aarch64 的 `libonnxruntime.so`，把产物取回；
  2. 找社区已编译好的 OHOS 版 `libonnxruntime.so`（AtomGit/GitCode 制品）；
  3. 给本机配代理（这一步同时解开 M5 端口③）。

- **M6 spike 现状**：第 2 条 ✅（klib 200）；第 1 条编译半段 ✅（`libkn.so` + 导出符号），
  但"装设备"半段 ⛔ 需签名；第 3 条 ❌ 网络阻塞；第 4 条 ⛔ 需华为开发者账号。
  **按 §4 D2，四条全通才进 M7**；剩余阻塞均属"需要 owner 决策/外部条件"，不再消耗轮次硬撞。

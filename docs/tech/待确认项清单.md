---
title: 待确认项清单
layer: 评价层
owner: SEKB Team
status: active
version: v1.0.0
last-updated: 2026-09-16
based-on-commit: 44dfed1
related: [11-EVOLUTION]
---

# 待确认项清单（需用户/架构决策拍板）

> 2026-09-16 复核：**Q-03 / Q-04 已定案并落地**（见下）；其余仍待决策。
> 每条状态标在首列。

| # | 状态 | 决策点 | 上下文 | 选项建议 |
|---|------|--------|--------|---------|
| Q-01 | ⏳ 待定 | 知识入库改为后台执行，还是保持同步（改注释）？ | `chat.py:320-325` 注释写「不阻塞主回复」但实为内联 `await`（漂移 D-01） | 建议后台 `create_task` + 入库状态由轮询/回调确认 |
| Q-02 | ⏳ 待定 | 死配置清理 or 接线？ | 已删 11 键（D3 批）；**仍有** `l3_knowledge.eviction` 三项、`tools.image_analysis`、`security.pii_masking` 无读取方（漂移 D-03/D-04/D-06） | 建议分三批：明显无用删；预算/淘汰/限流接线；预留保留（另见范围 2 的 A8/A9） |
| Q-03 | ✅ 已定案 | ~~RateLimitMiddleware 是否恢复挂载？~~ | 2026-09-10 起已启用（`enabled: true`），2026-09-15 资讯读写限流分离 | 已按「恢复挂载 + nginx 兜底」执行 |
| Q-04 | ✅ 已定案 | ~~预算控制（cost_control）是否本期落地？~~ | 占位键已删，现仅保留 pricing / usd_to_cny / usage（`bootstrap.py:224-230` 有接线） | 已决定**不做**预算控制/自动降级（漂移 D-05 关闭） |
| Q-05 | ⏳ 待定 | ChromaDB 自动淘汰（eviction）是否实现？ | `config.yaml:143-146` 三项仍无读取方（D-03） | 建议加 `max_items` 淘汰或保留人工重灌方案 |
| Q-06 | ⏳ 待定 | mypy 存量债清理节奏？ | 回归门禁已设；当前 306（上限 310） | 逐模块清零，每轮下调基线 |
| Q-07 | ⏳ 待定 | JSON→SQLite/Postgres 迁移优先级？ | ADR-02/03；postgres 已预留未启用 | 建议与多 worker 一起排期（多 worker 已决定不上，故顺延） |
| Q-08 | ⏳ 待定 | 前端 `no-explicit-any` 是否批量收窄？ | 见 10-TESTING | 建议新代码零 any，存量随重构收 |
| Q-09 | 🔶 部分定案 | postgres/redis 容器保留 or 移除？ | **redis 已在用**（`cost_control.usage` 的用量/费用统计）；postgres 仍已定义未启用 | 建议保留（演进预留）但注释标注 |
| Q-10 | ⏳ 待定 | 视觉模型（qwen-vl-plus 独立 client）是否并入 llm_factory？ | T3 漂移：仅剩 `image_processor` 独立视觉模型 | 建议统一（成本/统计） |

> 以上项同步登记于 docs/BACKLOG.md（活跃待办），决策后更新。

## 相关文档

- [11-EVOLUTION.md](./11-EVOLUTION.md)
- [docs/BACKLOG.md](../BACKLOG.md)

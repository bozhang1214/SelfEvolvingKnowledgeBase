---
title: 文档-代码冲突清单（漂移）
layer: 评价层
owner: SEKB Team
status: active
version: v1.1.0
last-updated: 2026-09-16
based-on-commit: 44dfed1
related: [11-EVOLUTION, docs/BACKLOG]
---

# 文档-代码冲突清单（既有文档 vs 代码 / 代码内部注释 vs 实现）

> 本清单记录逆向分析中发现的「文档/注释/配置声明 与 实际代码行为」不一致之处。
> 依据：事实表 T1-T8 抽取 + 代码阅读。每条含代码证据；改进归 11-EVOLUTION / BACKLOG。
>
> **状态列**：✅ 仍存在 ｜ ☑️ 已修复 ｜ 🔶 部分收窄。
> 2026-09-16 复核：12 条中 **5 条已修复、1 条部分收窄、6 条仍存在**（逐条实测见下）。

| # | 冲突类型 | 位置 | 声明 | 实际 | 状态（2026-09-16 复核） |
|---|---------|------|------|------|------|
| D-01 | 注释 vs 实现 | `chat.py:320-325` | 「不阻塞主回复」 | 知识入库为**内联 `await`**，仍在回包路径上 | ✅ 仍存在 |
| D-02 | 注释 vs 配置 | `auth.py` / `config.yaml:302` | JWT「72 小时」 | 现为 `token_expire_hours: 168`（7 天），auth.py 已无该过期注释 | ☑️ 已修复 |
| D-03 | 配置声明 vs 代码 | `config.yaml:143-146` `l3_knowledge.eviction` | 单用户条目上限/陈旧淘汰/相似度合并 | **无任何读取方**（`max_items` 的读取方是 `memory.l2_session`，另一处键；`eviction` 零命中） | ✅ 仍存在（属 A8/A9 死配置治理） |
| D-04 | 配置声明 vs 代码 | `config.yaml:239-244` `image_analysis` | yaml 配置视觉模型 | 实测 `ToolsConfig.model_fields` **不含 `image_analysis`**，且 `extra` 未设 forbid → 静默丢弃 | ✅ 仍存在（同 A8/A9） |
| D-05 | 配置声明 vs 代码 | `cost_control` 预算 4 项 | 预算控制/自动降级 | 占位键已删；`cost_control` 现余 pricing / usd_to_cny / usage，且 `bootstrap.py:224-230` 有接线 | ☑️ 已修复 |
| D-06 | 配置声明 vs 代码 | `security.*` | 注入防护/输入限制未实现 | 4/5 已实现（`guard.py` + `chat.py`/`share.py` 读 `prompt_injection_guard` / `max_input_length` / `blocked_patterns` / `prompt_injection_use_llm`）；**仅 `pii_masking` 仍无读取方** | 🔶 部分收窄（余 `pii_masking`） |
| D-07 | 文档 vs 代码 | `config.yaml:7` 注释 | 指向旧路径 | 已改指 `docs/tech/06-CONFIG-REFERENCE.md` | ☑️ 已修复 |
| D-08 | 指标 vs 埋点 | `metrics.py:328` `record_llm_call` | 定义但从未写入 | 已被 `core/llm_factory.py:597` 调用 | ☑️ 已修复 |
| D-09 | 指标标签 | `metrics.py:18,258,298,323` | user/tool 维度 | 仍硬编码 `user_id="default"` / `tool="web_search"` | ✅ 仍存在 |
| D-10 | 注释 vs 实现 | `scheduler trigger_now` | 可手动触发 | 全仓已无 `trigger_now`（函数已删） | ☑️ 已修复 |
| D-11 | 路由注册日志 | `server.py:326-333` | 日志列出 routers | 日志列 **12** 项，实际 `include_router` **13** 个 → 缺 `profile` | ✅ 仍存在 |
| D-12 | 配置语法 | `config.yaml:17` `${VAR:-default}` | 不支持默认值，恒空串 | `_resolve_env_ref` 已支持；实测 `${NONEXIST_TEST:-fallback}` → `'fallback'` | ☑️ 已修复 |

## 文档工程层漂移（2026-09-16 追加）

> 来源：文档工程检视 + 云平台接入复核。这些不是「代码 vs 文档」，而是**文档自身**的失真。

| # | 类型 | 位置 | 问题 | 状态 |
|---|------|------|------|------|
| DOC-01 | 文档互相矛盾 | `docs/ops/06`、`docs/ops/07` | 相对链接把 `../deploy/...` 写成 `docs/deploy/...`（共 7 处），实际断链 | ☑️ 已修复 |
| DOC-02 | 文档互相矛盾 | `docs/codeReview/.../Qoder/02` | 相对路径少一层（`../../tech/` → 应为 `../../../tech/`），2 处断链 | ☑️ 已修复 |
| DOC-03 | 工具误报 | `docs/tech/.validation/check-links.sh` | 缺 `../*/*.md` 分支，把**正确链接**（`docs/tech/ → ../ops/x.md`）误报为断链 | ☑️ 已修复 |
| DOC-04 | 文档失真 | `docs/tech/00-README.md:92` | 声称 pre-commit 钩子是 `.validation/pre-commit-check.sh`（**该文件不存在**）；真实钩子为 `doc-sync-check` → `check-doc-sync.sh`，且默认只提示不阻断 | ☑️ 已修复 |
| DOC-05 | 索引缺失 | `docs/tech/*` | 全库 **`jobcopilot` 零命中** —— 项目最重要的架构关系在内核接入层没有技术说明 | ☑️ 已修复（新增 `03-MODULES.md §11.5`） |
| DOC-06 | 文档失真 | `docs/tech/05-API-REFERENCE.md` | 仍写 `ChatRequest` 有 `skill` 字段（该模式已整体移除）→ 会误导接口调用方 | ☑️ 已修复 |
| DOC-07 | 行号漂移 | 4 处引用 | `UserProfile` 因 docstring 增行从 33-42 变为 35-44 | ☑️ 已修复 |
| DOC-08 | 工具窄覆盖 | `docs/tech/.validation/check-links.sh` | 只扫 `docs/tech/0*.md`，其余目录的「✅ 通过」是假绿 | ✅ 仍存在（属 D15 文档守卫任务） |
| DOC-09 | 治理缺口 | `docs/tech/.validation/check-freshness.sh` | **未接入 pre-commit/CI**，过期告警不会自动出现 | ✅ 仍存在（属 D15） |
| DOC-10 | 文档失真 | 云平台接入手册 | 工具数写「6 个」，实际 7 个（新增 `self_check`）；调研日期写成 2026-02（实为 2026-09-15） | ☑️ 已修复（内核 `7a6f8a7`） |
| DOC-11 | 事实表落后 | `docs/tech/.facts/T1/T2/T8` | 逆向分析期工作产物已过期：T1 记 65 条端点（实测 **75**）、T2 记 178 叶键（配置已增删多轮）、T8 的 health job / LocalTraceCollector / `record_llm_call` 均与现状不符 | 🔶 已加统一过期横幅（不逐表重跑；以代码 + `docs/tech/*.md` 为准） |

## 技术文档复核（2026-09-16）

> **背景**：`docs/tech/00–11` 全部 12 篇的 `based-on-commit` 都停在 **2026-09-09**
> （落后 HEAD 190–203 个提交），`01-ARCHITECTURE` / `02-RUNTIME-FLOWS` 已越过
> `check-freshness.sh` 的 `LOG_DEPTH=200` 阈值。任务登记见 `docs/BACKLOG.md` **W1**。
>
> 复核方式：逐篇把「模块名/路径/`file:line` 引用/端点/配置键/指标名/测试命令」与当前代码核对，
> 修正后把 `last-updated` / `based-on-commit` 推进到复核时的 HEAD。

| 文档 | 结论 | 主要修正 |
|---|---|---|
| 00-README | ☑️ 已复核 | 钩子名与阻断语义、`check-links.sh` 覆盖范围的诚实说明、`.validation/` 与内核接入入口 |
| 01-ARCHITECTURE | ☑️ 已复核 | 见下方分篇记录 |
| 02-RUNTIME-FLOWS | ☑️ 已复核 | 见下方分篇记录 |
| 03-MODULES | ☑️ 已复核 | 新增 §11.5 JobCopilot 内核接入（DOC-05） |
| 04-DATA-MODEL | ☑️ 已复核 | 见下方分篇记录 |
| 05-API-REFERENCE | ☑️ 已复核 | 见下方分篇记录 |
| 06-CONFIG-REFERENCE | ☑️ 已复核 | 见下方分篇记录 |
| 07-DESIGN-PATTERNS | ☑️ 已复核 | 见下方分篇记录 |
| 08-GLOSSARY | ☑️ 已复核 | 见下方分篇记录 |
| 09-OBSERVABILITY | ☑️ 已复核 | 见下方分篇记录 |
| 10-TESTING | ☑️ 已复核 | 见下方分篇记录 |
| 11-EVOLUTION | ☑️ 已复核 | 见下方分篇记录 |

### 分篇复核记录

> 复核基准：**`44dfed1`**（复核期间未再改后端代码；各篇 `based-on-commit` 均已推进到该 commit）。
> 每篇都做了「逐条把声明与代码核对」，下面只列**主要**修正，不是全部 diff。

**01-ARCHITECTURE**
- LLM 口径：`deepseek-chat`/`deepseek-reasoner` 快慢分级 + 9 角色 → **11 个角色全部 `deepseek-flash`**（`config.yaml:27-82`，`7852aae` 起统一）；
- RSS 源「15 个」→ **38 条**（`config.yaml:336-379`）；
- **容器表缺 `jobcopilot-mcp`**（`compose:268-320`，`127.0.0.1:8765`）→ 补行 + C4-L2 节点 + 「JD 分析委托内核」关系；
- redis「预留未启用」→ **已启用**（L2 记忆 + 用量统计）；postgres 仍预留（`profiles:["with-db"]`）；
- 分享路径 `/share` → **`/sekb/share/*`**（`main.tsx:11` basename）；JWT 90 天 → 7 天；
- 证据行号全面重锚（bootstrap / json_storage / builder / compose / 博查 web_search）。

**02-RUNTIME-FLOWS**
- **`chat.py` 全篇行号失效**：原文按约 835 行旧版，实际 675 行 → §1/§2/§4-§8 引用逐条重锚（如 `event_generator` `:698-835` → `:488-620`）；
- 有一处引用了**不存在的行号**（`state.py:498-519`，该文件仅 244 行）→ 实为 `chat.py:225-246`；<!-- check-docs:ignore 历史错例，刻意保留原文以说明当时错在哪 -->
- `on_token` 传递方式：不是 create_task 参数，而是 **token sink contextvar**；
- 模型名过期（见 01）+ 「reasoner→chat 降级」**当前不可达**（`_should_fallback` 只在模型名含 `reasoner` 时生效）；
- 补上遗漏的 **`rewrite` 重写分支**；反思策略由 always/adaptive/sampling → **实际只有 always**。

**04-DATA-MODEL**
- `KnowledgeEntry` **补 `topic` 字段**；`metadata` 16 → **17 个标量**；补各字段默认值；
- 资讯 `_cleanup` 由「只删 `daily_*.md`」→ **md + json 同删**（`storage.py:135-151`）；
- 招聘缓存：「均无锁非原子写」→ 两个 cache **已原子写**，仅 archive `_save_index` 非原子；补 `_MAX_ENTRIES=200` 与 analysis_cache 画像指纹；
- 分享默认 30 → **7 天**；补 `view_count`/`last_accessed_at`；
- `users.json` 字段口径（`password_hash/settings/is_active`，`access_level` 是 `UserPublic` 派生）且已是原子写；§8 l2_session 由「未启用」→ 已启用 Redis。

**05-API-REFERENCE**
- 实测 **75 个端点**（原文按 65 条），**补 9 个缺失端点**（`auth/change-password`、`auth/reset-password`、`chat/usage`、`news/status`、`job/searches`、`job/cache/search/{id}`、`job/cache/list`、`job/apply-plan`×3）；
- `share.expires_days` 默认 30 → **7（`ge=-1,le=30`）**；批量分析缓存 7 → **14 天**；
- `conversations` 契约修正：`GET /{id}` 实为 `{conversation, messages}`、无 `limit` 参数、`POST` 的 `title` 是**查询参数**；
- `knowledge` PATCH 响应字段重写；`§15` 两条安全结论纠正（`knowledge/categories` 受 router 级 `require_full_access` 约束）；
- 全表 evidence 行号重定位（旧引用偏移 20~350 行，upload 段甚至超出文件长度）。

**06-CONFIG-REFERENCE**
- `${VAR:-default}` 由「不支持、恒空串」→ **实际支持**（`_resolve_env_ref`，实测通过）；
- `rate_limit.*` 由「死配置」→ **已启用生效**（含 news 读/写分离 120/6）；`memory.l2_session` 由「全死」→ **已接线**；
- `security` 由「全死」→ **4/5 已接线**（仅 `pii_masking` 无读取方）；`cost_control.pricing` 键名 → `deepseek-flash` + `usage.*`；
- `llm.roles` 9 → **11 角色**；`token_expire_hours` 2160 → **168**；
- **死键重算为 29 个**；新增/删除多个键；大量 evidence 行号校正（含 `eval/metrics.py` → `eval/assertion.py`、图片配置 → `services/upload_service.py`）。

**07-DESIGN-PATTERNS / 10-TESTING**
- 07：策略目录「always/adaptive/sampling」→ **仅 always**（占位已删）；`chat.py` 规模 `828→538` → **`828→675`**；单测 `528` → **841**；services `7` → **9**；可观测性「4 指标未埋点」→ **3 个**（并列出指标名）；「49 死配置」→ **29 个**（与 06 对齐）；
- 10：后端单测 `528 用例（29 文件）` → **825 用例（55 文件）**（总数 841 − 集成 16）；前端 `64` → **68**；CI 触发由「main + 定时」→ **仅 push 到 main**；mypy `310` → **306（基线 310）**；覆盖率 43%/44% → **实测 54%**（`pytest tests/unit --cov=app`，2026-09-16）。

**08-GLOSSARY**
- 图谱/状态行号全部重锚（`builder.py:288-362→305-407`、`state.py` 多处）；L2 由「死配置/未启用」→ **已启用 Redis**；
- `thinking`/`token` 事件引用修正（原文引 `chat.py:710-728`，该文件仅 675 行）→ `500-514 / 217-220`，措辞改「真流式」；<!-- check-docs:ignore 历史错例 -->
- MCP 条目由「仅 web_search 用 stdio」→ **jobcopilot 内核 stdio 7 工具**（web_search 走博查 HTTP，不经 MCP）；JWT 条补有效期。

**09-OBSERVABILITY / 11-EVOLUTION**
- 09：删「health 30s 独立抓取」（`prometheus.yml:39-41` 明确不单独抓）；`sekb_llm_call_latency_seconds` 由「从未写入」→ **现役**（`llm_factory.py:597`）；未写入指标 4 → **3**；trace 的 `LocalTraceCollector` **已按 D6 删除**，缺口关闭；补 `MONITOR_BIND_IP`、feishu 仅绑回环、30d/10GB、Loki 336h；
- 11：TD-05 4→3 指标且已接线；TD-06 收口 9/10（仅剩 image_processor 直连）；TD-08 「无 jti 黑名单」→ **已实现**（`auth.py:26-29,93-103`）；TD-09 行号重锚；§2.1 限流项标 ✅。

**跨篇一致性（复核中发现的互相矛盾，已一并收口）**
- 指标未埋点数 07 写 4 / 09 写 3 → 统一为 **3**（并列出名字，实测 23 个指标中 3 个无写入方）；
- 死配置数 07 写 49 / 06 重算 29 → 统一为 **29**；
- 覆盖率 10 写 43% / `ci.yml` 注释写 44% → 统一为**实测 54%**；
- JWT 有效期 三处不一致（`config.yaml:302`=168h、`config.py` 默认 2160、前端 `auth.ts` 注释 90 天）→ 后两处已改为 **7 天**（`config.py` 默认值同时收紧，避免 yaml 漏配时静默回落到 90 天）。

## 历史旧文档 vs 新文档

- 历史审查存档见 `docs/codeReview/`（已从旧 docs 迁移）。
- codeReview 报告（Qoder 等）中已修复项（JWT/并发/端口等）不再计入；未修复项转入 11-EVOLUTION 与 BACKLOG。

## 相关文档

- [11-EVOLUTION.md](./11-EVOLUTION.md)
- [docs/BACKLOG.md](../BACKLOG.md)
- [03-MODULES.md](./03-MODULES.md) §11.5（内核接入）

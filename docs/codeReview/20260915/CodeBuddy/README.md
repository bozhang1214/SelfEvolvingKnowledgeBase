# 2026-09-15 全量审查（CodeBuddy）

## 交付物

| 文件 | 内容 | 条目数 |
|---|---|---|
| `01-代码问题审查.md` | 全量代码缺陷，按严重程度分级 | 50 条（严重 7 / 高 17 / 中 18 / 低 8） |
| `02-文档优化建议.md` | 文档体系优化，按 A/B/C/D 四类 | 30 条 |
| `03-使用者视角功能优化建议.md` | 破除"知识的诅咒"的体验优化 | 20 条 |

## 审查范围

`backend/app`（198 个 py）、`frontend/src`、`backend/config.yaml`、`deploy/Dockerfile`、`README.md`、`AGENTS.md`、`docs/`（`tmp/` 与 `codeReview/` 除外）、`prompt/`。

## 一句话结论

- **代码**：能力完整度高，但有 4 类系统性缺陷需在架构层统一治理——异常原文回传、存储无锁非原子写、数据目录硬编码、配置与代码不同步。
- **文档**：处于"完成大迁移但旧文档没清干净"的中间态；**四类受众中使用者几乎缺位**（tech 15 篇 + ops 16 篇，用户手册 0 篇）。
- **使用**：系统不缺能力，缺的是"把能力解释给用户听"的那层——错误信息不可读、系统状态不可见、首用无引导。

## 最该先做的 5 件事

1. 去掉 `--forwarded-allow-ips "*"`（当前全站限流可被一行请求头绕过）
2. 注册全局异常处理器，消灭 `detail=f"...{e}"`（约 20 处）
3. `user_storage` 改用原子写（当前崩溃即丢全部用户）
4. 修 README 三大过期项：项目结构树、启动命令、API 清单
5. 建 `docs/user/` 用户手册 + 预览权限显式告知

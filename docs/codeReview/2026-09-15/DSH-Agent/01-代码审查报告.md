# 代码审查报告（全量代码）

> 审查者：DSH-Agent ｜ 日期：2026-09-15
> 范围：backend/app、frontend/src、deploy、docker-compose*.yml、scripts、jobcopilot 内核（子模块）
> 方法：只读静态审查 + 部分问题实机核验；前端/基础设施部分由并行审查子任务产出，后端专项由本会话一手核验。
> 严重度：P0（安全/崩溃/数据丢失，立即）｜P1（重要 bug/性能/可维护，近期）｜P2（次要/卫生，可后置）

---

## 汇总

| 严重度 | 数量 |
|---|---|
| P0 | 2 |
| P1 | 13 |
| P2 | 18 |
| **合计** | **33** |

> 注：后端专项审查子任务仍在运行，其额外发现将作为附录追加。

---

## P0（立即处理）

| # | 类别 | 问题 | 证据 | 建议 |
|---|------|------|------|------|
| 1 | 安全 | 监控栈零鉴权：Grafana 默认凭据 `admin/admin`（`${GRAFANA_ADMIN_PASSWORD:-admin}`），Prometheus/Loki/Alertmanager 全无认证且端口绑 `0.0.0.0`；Loki 聚合全部容器日志，任一端口可达即泄露日志/指标 | `docker-compose.monitoring.yml:64-65,42,78,105,152`；`pre-deploy-check.sh:200` 对 ufw 未启用只 warn 不 fail | 随机密码 + 绑 `127.0.0.1` 或 basic-auth；pre-check 升级为 fail |
| 2 | 安全 | `deploy/backup.sh`（虽标废弃但 cron 示例仍指向它）把 `.env.prod` 明文打进备份并 s3 sync 上传——DEEPSEEK_API_KEY/JWT_SECRET/飞书 SECRET 明文落远程；JWT 密钥泄露 = 可伪造任意用户 token | `deploy/backup.sh:120,140,20` | 停用并删除；备份排除 `.env.prod` 或加密 |

## P1（近期处理）

| # | 类别 | 问题 | 证据 | 建议 |
|---|------|------|------|------|
| 3 | 安全 | JWT token 存 `localStorage`（续租同样），XSS 可窃取长期 token | `frontend/src/services/auth.ts:7,58` | 改 httpOnly Cookie（或至少缩短有效期+CSRF 防护） |
| 4 | 安全 | 飞书网关 `/webhook`、`/test` 无鉴权，`5001` 绑 `0.0.0.0`——可达者可伪造告警、灌飞书群 | `deploy/feishu-webhook/feishu_gateway.py:238,268`；`docker-compose.monitoring.yml:189` | 加密钥校验 + 绑回环 |
| 5 | 功能 | 401 时 `window.location.href='/login'` 硬编码绝对路径，不尊重 `/sekb` basename——生产登录态过期跳转到不存在的 `/login` | `frontend/src/services/api.ts:74` vs `main.tsx:11` | 改 `/sekb/login` 或 navigate |
| 6 | 健壮性 | 前端 SSE fetch 无超时/无重连/无重试，`response.body!` 非空断言——服务端不结束流则永久「生成中」，断线无恢复 | `frontend/src/services/chat.ts:57-73` | 加超时/心跳/重连 |
| 7 | 健壮性 | 会话 store 多处 `catch{}` 静默吞错（load/select/create/delete/rename/togglePin），失败用户不可见、无日志 | `frontend/src/stores/chat.ts:205,218,232,258,271,289` | 统一错误提示 + 日志 |
| 8 | 数据丢失 | jobcopilot `json_store.save()` 非原子写（无 tmp+rename），`load()` 损坏静默返回 `{}`——写一半崩溃即损坏且无感知 | `jobcopilot/src/jobcopilot/storage/json_store.py:51-56,48-49` | tmp+`os.replace` + 损坏告警 |
| 9 | 正确性 | `analyze_market(min_salary_k=…)` 参数**不生效**：采集器用 `cfg.default_min_salary_k`，参数只进缓存键/报告缓存——调用方以为按薪资过滤，实际没过滤 | `backend/app/agents/job/market.py:276` vs `:252,263,285` | 采集器改传该参数（或明确语义） |
| 10 | 健壮性 | `get_app_context()` 模块级 `_app_context` 未初始化为 `None`——在 `initialize_app()` 前调用抛 **NameError** 而非声明的 RuntimeError（本会话 smoke 实测确认） | `backend/app/core/bootstrap.py:354-358`（缺模块级 `_app_context = None`） | 模块级初始化 `None` 并抛 RuntimeError |
| 11 | 成本 | 聊天 `search_jobs` 工具无成本/限流护栏——走 `/api/v1/chat/` 前缀，绕过 `/api/v1/job/` 的 job_per_minute 限流组，可反复触发昂贵批量分析 | `backend/app/agents/executor.py:250-253` | 加每日上限或复用 job 限流 |
| 12 | 数据 | 资讯 `index.json` 非原子写，损坏后静默返回空列表 → 全量索引丢失且无告警 | `backend/app/agents/news/storage.py:127` | tmp+rename+锁+告警 |
| 13 | 功能 | 前后端哈希口径不一致：前端 `computeFileHash` 用 SHA-256，注释却称「与后端 MD5 对齐」——「同名同内容跳过」去重永不命中 | `frontend/src/services/file.ts:242,292-303` | 统一哈希口径 |
| 14 | 安全 | CSP 含 `script-src 'unsafe-inline'`，削弱防 XSS | `deploy/nginx.conf:101` | 去 unsafe-inline（Vite 产物外部脚本无需内联） |
| 15 | 安全 | 邮箱+新密码即可重置账号，无验证码/二次确认——知晓邮箱即可接管 | `frontend/src/pages/Login.tsx:21`、`services/auth.ts:98` | 加邮箱验证码/限流 |

## P2（可后置）

| # | 类别 | 问题 | 证据 | 建议 |
|---|------|------|------|------|
| 16 | 类型 | 前端全仓 99 处 `any`/`Record<string,any>`，LLM 输出放弃类型校验 | `features/job/render.tsx:132,220,322` 等 | 增量治理 noImplicitAny |
| 17 | 性能 | Chat 消息列表无虚拟滚动，逐 token 全量重渲染，长对话卡顿 | `frontend/src/pages/Chat.tsx:535` | 虚拟列表 + token 节流 |
| 18 | 部署 | 多处镜像 `:latest`（prometheus/grafana/loki/promtail/alertmanager/rsshub 等）——tag 漂移、供应链风险 | `docker-compose.monitoring.yml`、`prod.yml:164` | 固定版本 |
| 19 | 安全 | postgres 默认密码 `change-me-to-a-strong-db-password`、redis 无 `--requirepass`——仅靠「未映射端口」防护 | `docker-compose.prod.yml:222,243` | 强密码 + 认证 |
| 20 | 部署 | 单文件 bind mount（`./deploy/nginx.conf`、`/etc/letsencrypt`）——git pull 更新/certbot 续签后 nginx 不自动 reload | `docker-compose.prod.yml:137,139` | 部署后 reload 或改目录挂载 |
| 21 | 健壮性 | jobcopilot `int(os.environ.get("JOBCOPILOT_PORT","8765"))` 无保护——env 非数字时 ValueError 崩，MCP 起不来 | `jobcopilot/src/jobcopilot/mcp/config.py:260` | try/except + 可读报错 |
| 22 | 并发 | jobcopilot LLM 缓存键 sha256 前 16 hex 截断 + 模块级 OrderedDict 无锁——哈希碰撞共享客户端、并发隐患 | `jobcopilot/src/jobcopilot/mcp/request_keys.py:145,40` | 加锁 + 完整键 |
| 23 | 安全 | Gitea 端口 3000/2222 绑 `0.0.0.0`，仅靠 ufw 单点防护 | `docker-compose.monitoring.yml:233-235` | 绑回环/内网 |
| 24 | 部署 | `deploy/deploy.sh:112 run()` 用 `eval "$@"`——参数可控时有命令注入面（当前硬编码，风险低） | `deploy/deploy.sh:112` | 去 eval |
| 25 | 部署 | `deploy/restart.sh` 全程无 flock（对比 deploy.sh 有锁）——并发交错重启半成品 | `deploy/restart.sh` | 加 flock |
| 26 | 一致性 | `NewsAgent.write_status` 写 `_storage._dir/last_status.json`，`read_status` 读 `_config.report_dir/last_status.json`——当前相等但语义漂移风险 | `service.py:282` vs `:303` | 统一用同一路径来源 |
| 27 | 并发 | 资讯 `.lock_*` 写入 PID 但陈旧判定用 mtime>600s 而非 PID 活性——慢任务>10min 被误判陈旧、崩溃进程锁阻塞 10min | `service.py:103,109,260` | 校验 PID 存活 |
| 28 | 可维护 | `RateLimitMiddleware._parse_route_key` 把全大写首 token 一律当 HTTP 方法——前缀若全大写会被误判 | `middleware.py` | 加方法白名单 |
| 29 | 健壮性 | `fire_and_forget` 在无事件循环时丢弃告警（旁路能力可接受，但应记日志说明「告警已丢弃」） | `core/alerts.py` | 已有日志，确认覆盖 |
| 30 | 性能 | `web_search` 无缓存，跨轮重复查询重复付费 | `tools/registry.py`、`executor.py` | TTL 缓存 + 命中指标 |
| 31 | 性能 | 同步文件 IO（job 缓存/archive/market 读写）跑在 async 路由 | `market.py`、`archive.py`、`job_cache.py` | `asyncio.to_thread` |
| 32 | 测试 | 资讯/职位/画像/分享分类等新增功能测试覆盖不足（部分仅 stub） | `backend/tests/unit` | 补关键路径测试 |
| 33 | 可观测 | news/job 两子系统仍缺 metrics（仅 logger，本会话补了状态落盘与告警，指标未补） | `agents/news`、`agents/job` | 补 `news_*`/`job_*` 指标 |

---

## 已核验「曾经存在、现已修复」的问题（避免重复报告）

- chat 会话越权（IDOR）：`chat.py:163-164` 已加 `conv.get("user_id") != user_id` 归属校验 ✅
- news/job 裸 `ainvoke` 绕过 stats：已迁 JobCopilot 内核 + `_log_kernel_usage` 用量回报 ✅
- 全局限流关闭：已启用 + 资讯读写分离 ✅
- 主聊天伪流式：已 `astream_events` 真流式 ✅

（后端专项审查子任务的补充发现将追加在文末。）

# SEKB 全量审查问题总表（去重后，含状态）

> 汇总：DSH-Agent ｜ 日期：2026-09-16
> 来源：DSH-Agent / Trae(原DeepSeek) / Qoder / CodeBuddy 四家共 11 份报告，**去重合并**
> 状态：✅ 已修复 ｜ 🔧 本轮新增修复 ｜ ⏸️ 暂缓（待决策/较大改动）｜ ⛔ 不需修复（已修/误报/不适用）
> 数量：**安全 24 · 正确性 20 · 数据并发 13 · 资源性能 20 · 架构可观测 19 · 文档 27 · 使用体验 37 = 160 条**

---

## 一、安全（24）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| S1 | 重置密码仅凭「已登录 + 知道邮箱」即可改任意账号密码，无验证码/旧密码确认 | 账号接管链（配合限流绕过） | 先加原密码确认/仅限本人；邮箱验证码列后续 | ✅ |
| S2 | 图片原图保存时 `file_name` 未消毒，`../` 可写出上传目录 | 任意路径写文件 | 复用 `safe_rel_path` | ✅ |
| S3 | `verify_password` 用 `==` 比较哈希（非常量时间） | 计时侧信道 | `hmac.compare_digest` | ✅ |
| S4 | `guard` 解析失败用 `"true" in s` 猜测 | 正常输入「if true」被误判注入拦截 | 只认 JSON 字段 `injection` | ✅ |
| S5 | `user_storage._save` 直接覆盖写 | 崩溃截断 → 用户表损坏 → 全站无法登录 | tmp + `os.replace` | ✅ |
| S6 | 上传先 `file.read()` 全量入内存再判 50MB | 单请求即可 OOM（DoS） | 分块读+超限中断 | ✅ |
| S7 | JWT 有效期 90 天 + 前端「自动续租」 | 泄露后风险窗口极大 | 缩短（7 天）或 access+refresh 双 token | ✅ |
| S8 | jti 黑名单为进程内存 set | 重启/多 worker 后登出失效 | 迁 Redis；已加 TTL 清理防无界 | ⏸️ |
| S9 | token 存 `localStorage` | XSS 可窃取长期 token | 改 httpOnly Cookie（需 CSRF 防护） | ⏸️ |
| S10 | 401 跳转硬编码 `/login`，未带 `/sekb` basename | 生产登录态过期 → 404 | 用 `import.meta.env.BASE_URL` | ✅ |
| S11 | 监控栈零鉴权（Grafana admin/admin、Prom 等无认证，绑 0.0.0.0） | 任一端口可达即读全部日志/指标 | 随机强密码 + 绑回环（需确认访问方式） | 🔧 |
| S12 | browser-service `/cookies` 等端点零鉴权 + cookie 明文落盘 | 同网容器可劫持 BOSS 登录态 | 内部共享 token + `chmod 600` | ✅ |
| S13 | 分享链接无密码/无过期/无访问审计，SSE 问答无限流 | 泄露即被反复读/刷爆 LLM 成本 | 默认 7 天过期 + 访问统计 + 分享维度限流 | 🔧 |
| S14 | postgres 弱默认口令、redis 无 `--requirepass` | 内网可达即裸奔 | 强密码 + 认证 | ⏸️ |
| S15 | PBKDF2 100k 迭代低于 OWASP 600k | 离线爆破成本偏低 | 提高迭代（登录时惰性重哈希） | ⏸️ |
| S16 | CORS `allow_origins=["*"]` + `allow_credentials=True` | 任意源可带凭据请求 | 显式白名单 | ✅ |
| S17 | 业务路由 `f"...{e}"` 异常原文回传客户端（20+ 处） | 泄露内部路径/表名/组件信息 | 全局异常处理器统一脱敏 + error_id | ✅ |
| S18 | `--forwarded-allow-ips "*"` 信任任意 XFF | （原）可伪造 IP 绕过限流/污染审计 | nginx 层 `$remote_addr` 覆盖 XFF 已根治 | ⛔ |
| S19 | Gitea 端口 3000/2222 绑 0.0.0.0 | 边界防护单点 | 绑回环/内网 | ⏸️ |
| S20 | `audit.get_client_ip` 直接信任 XFF 首值 | 审计来源可伪造 | 与反代信任策略统一 | ⏸️ |
| S21 | 知识入库 / 分享问答的 prompt injection 未清洗 | 知识纯净度/回答可信度 | 建立清洗/转义策略 | ⏸️ |
| S22 | 备份脚本把 `.env.prod` 明文打包上传 S3 | 密钥泄露（JWT=可伪造 token） | 停用或排除/加密 | ⏸️ |
| S23 | 邮箱枚举（注册/重置对未知邮箱响应不同） | 探测已注册用户 | 统一响应 | ⏸️ |
| S24 | config.yaml `jwt_secret` 保留弱默认字面量 | 误导部署者（实际已被启动强校验拒绝） | 删该行或标注废弃 | ✅ |

## 二、正确性（20）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| C1 | `analyze_market(min_salary_k)` 只进缓存键，采集器用配置默认值 | 调用方以为按薪资过滤，实际没过滤 | 参数传采集器 | ✅ |
| C2 | `BossBrowserSource` 硬编码 `city="北京"` | 非北京用户永远采不到 BOSS 岗位 | 透传 city | ✅ |
| C3 | `save_job_cache` 默认关键词硬编码 `"Agent"`，与 fetch 分支不同源 | search_id 不同 → 职位串档/丢失 | 与 `config.job.default_keyword` 同源 | ✅ |
| C4 | `analysis_cache` key 用 `jd_text[:3000]` 截断 | 前 3000 字相同的不同 JD 命中同一缓存 → 错误分析 | 全文哈希 | ✅ |
| C5 | `analysis_cache` key 不含用户画像版本 | 画像更新后 14 天内仍返回旧画像结果，且无提示 | key 纳入 profile hash；画像更新即失效 | ✅ |
| C6 | `_app_context` 无模块级初始化 | 未初始化调用抛 `NameError` 而非声明的 `RuntimeError` | 模块级 `= None` | 🔧 |
| C7 | `market` 在 search_id 为空时回退「该用户最新报告」 | 跨搜索串数据 | 视为无缓存重算或强制必填 | ✅ |
| C8 | `_report_matched` 用严格集合相等 | 职位列表天然动态 → 几乎永远显示「报告已过期」 | 覆盖率阈值（≥80%）并显示比例 | ✅ |
| C9 | `job_cache` key 不含 page/limit/source | 第 2 页结果覆盖第 1 页 | key 加维度 | ✅ |
| C10 | `job_cache.parse_key` 用 `|` 分割 | 关键词含 `|` 时解析错乱 | 改 json key 或校验输入 | ⏸️ |
| C11 | job 列表 count 字段三来源语义不一 | 前端分不清「0 条」与「已过期」 | 拆 `job_count`/`expired` | ⏸️ |
| C12 | 前端 `computeFileHash` 用 SHA-256，注释称与后端 MD5 对齐 | 「同名同内容跳过」去重永不命中 | 统一哈希口径 | ⏸️ |
| C13 | `compute_md5` 实现实为 SHA-256 | 命名误导，第三方对账对不上 | 全链路改名 sha256 | ⏸️ |
| C14 | 流式调用 token/cost 恒为 0；内核调用只打日志 | 用量/配额/成本统计系统性偏低 | 用 `usage_metadata` 或按字符估算并标注 | ⏸️ |
| C15 | `record_chat_metrics` 硬编码 `user_id="default"` | 所有用户归并一个标签，指标失真 | 传真实 user_id（或 hash） | ⏸️ |
| C16 | `daily_cost_usd` 只增不重置，仓库无重置任务 | 跨天累加语义错误 | 按 UTC 日 label 或 scheduler 重置 | ⏸️ |
| C17 | `user_storage.create` 唯一冲突抛 500 | 同毫秒注册客户端看到服务器错误 | 重试生成新 ID 或返回 409 | ⏸️ |
| C18 | `job.py` 列表接口 N+1 全量 JSON 读写 | 搜索积累后接口明显变慢 | 一次 `_load()` 内存关联 | ⏸️ |
| C19 | 命中缓存时仅刷新时间戳却全量重写文件 | 写放大 + 放大竞态窗口 | 惰性刷新或节流 | ⏸️ |
| C20 | `upload` 返回值计数与实际不符（`reclassified`/`updated_files`/`entries_updated`） | 前端「成功 N 条」是错的 | 只在实际变更时计数 | ⏸️ |

## 三、数据与并发（13）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| D1 | `share_storage.append_message` 读-改-写跨 await 无锁 | 并发追加丢消息 | 按 (share,viewer) 加 `asyncio.Lock` | ✅ |
| D2 | `job_cache` 非原子写 | 崩溃截断后静默清空缓存 | tmp + replace | ✅ |
| D3 | `analysis_cache` 非原子写 | 同上 | tmp + replace | ✅ |
| D4 | `market` 报告缓存非原子写 | 同上 | tmp + replace | ✅ |
| D5 | 资讯 `index.json` 非原子写 | 损坏后静默返回空 → 全量索引丢失 | tmp + replace | 🔧 |
| D6 | `json_storage` 并发 append 无锁 | （报告称会丢消息） | **已有 `self._lock`** | ⛔ |
| D7 | `json_storage` 临时文件固定名 | 跨进程 tmp 竞态 | 单 worker 部署，无此场景 | ⛔ |
| D8 | `job_cache` 无容量上限，条目永不删除 | `job_cache.json` 无限膨胀 | LRU 淘汰到 N 条 | ✅ |
| D9 | 资讯 `_cleanup` 只删 `daily_*.md` 不删 `.json` | 结构化 JSON 无限堆积 | 一并清理 | ✅ |
| D10 | `archive` 全局截断 200（注释写「每用户 200」） | 用户互相挤掉 + 孤儿文件 | 按 user 分组截断并删文件 | ⏸️ |
| D11 | 上传 MD5 索引非原子全量读写；异步分支不写 MD5 | 去重永久失效/索引损坏 | 带锁原子写 + 补写 + 迁移 | ⏸️ |
| D12 | `chat_share_storage` 复制了不安全的 append 实现 | 同类并发丢消息 | 同 D1 处理 | ⏸️ |
| D13 | 会话删除后 memory 四字典不清理 | 内存缓慢增长 | 删除会话时联动清理 | ⏸️ |

## 四、资源与性能（20）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| P1 | 限流窗口 `_windows` 只清列表不删空 key | 伪造海量 IP → 内存泄漏 | get + 空桶删除 | ✅ |
| P2 | `_jti_blacklist` 只增不删 | 长跑内存增长 | dict + TTL 清理 | ✅ |
| P3 | 客户端断开时后台 `_run_chat` 未 cancel | 继续烧 token 并落库 | `finally` 里 cancel | ✅ |
| P4 | `stats.records` 无界 append | 慢速 OOM；`retry_rate` O(n) | deque(maxlen) + 单调计数器（需改 delta 机制） | ⏸️ |
| P5 | L1 短期记忆 4 个无界字典 + 每会话 Lock 不回收 | 内存增长；重启丢失；多 worker 不一致 | LRU/TTL 止血 → 中期 Redis | ⏸️ |
| P6 | `_QR_SESSIONS` 持 Playwright 上下文，不轮询即永久泄漏 | Playwright 资源耗尽 | 后台清理 + 过期分支 `dispose()` | ⏸️ |
| P7 | `browser_client` 每次调用新建 `AsyncClient` | 无连接复用、TLS 重复握手 | 模块级单例 + lifespan 关闭 | ⏸️ |
| P8 | 多模块 async 路径中同步阻塞 IO（chroma 初始化/shutil/tiktoken/缓存读写/audit 写） | 冻结整个事件循环（含 SSE） | `asyncio.to_thread` | ⏸️ |
| P9 | `upload /reclassify` 串行逐文件调 LLM | 100 文件易超时 | `gather` 限并发或后台任务 | ⏸️ |
| P10 | `/upload/files`、`/upload/series` 全量加载无分页 | 几千条后前端一次拉全量 | 加分页/游标 | ⏸️ |
| P11 | BM25 全量条目拉取 + CPU 密集跑在事件循环 | 阻塞其他请求 | 限规模 / to_thread | ⏸️ |
| P12 | 前端消息列表无虚拟滚动，逐 token 全量重渲染 | 长对话卡顿 | 虚拟列表 + token 节流 | ⏸️ |
| P13 | 前端无代码分割，首屏整包 2.05MB | 首屏慢 | React.lazy + manualChunks | ⏸️ |
| P14 | `_stream_tokens` 逐字符 yield + `sleep(0)` | 长回答数千次上下文切换 | 按 token/块推送 | ⏸️ |
| P15 | `usage_service` 10 次串行 Redis 往返 | P95 延迟 | pipeline/MGET | ⏸️ |
| P16 | `EmbeddingFactory`/`LLMFactory` 懒加载无锁 | 并发首请求重复初始化 | 加锁或 lifespan 预热 | ⏸️ |
| P17 | 资讯日报逐类串行 LLM（14~36 次） | 单次 10~20 分钟 | `gather` + 信号量 | ⏸️ |
| P18 | 资讯分类 prompt 无分批/上限 | 300 条 → 超长失败 → 静默退化关键词分类 | 分批并发 + 成功率指标 | ⏸️ |
| P19 | RSS 约 40 源无连接池/重试/熔断 | 单源抖动拖慢整体 | 共享 client + Semaphore | ⏸️ |
| P20 | 周/月报重抓全量，不复用已落盘日报 | 重复抓取 + 重复 LLM | 基于已存结构化日报聚合 | ⏸️ |

## 五、架构与可观测（19）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| A1 | 三套 job 缓存（job_cache/analysis_cache/market）未统一 | 膨胀 + 串档 + 各自为政 | 抽 `AtomicJsonStore`（锁+原子写+LRU） | ⏸️ |
| A2 | news/job 裸 `ainvoke` 绕过 `ainvoke_with_stats` | （原）成本 0/无重试 | **已迁内核 + 用量回报** | ⛔ |
| A3 | 主聊天「伪流式」 | （原）首 token = 全量延迟 | **已 `astream_events` 真流式** | ⛔ |
| A4 | 全局限流关闭 | （原）无防护 | **`enabled: true` 已启用** | ⛔ |
| A5 | chat 会话越权（IDOR） | （原）可读他人会话 | **已有 `user_id` 归属校验** | ⛔ |
| A6 | `conversations_total` 等「死指标」 | —— | **metrics.py 仍有定义，非不存在** | ⛔ |
| A7 | 多 worker 一致性问题（`_conv_inflight`/L1/Chroma/scheduler） | 多 worker 时失效 | 单 worker 部署下不触发；扩容前处理 | ⛔ |
| A8 | 死配置（eviction/cost_control/image_analysis 等） | 配置误导 | `extra="forbid"` 或删 | ⏸️ |
| A9 | `tools.image_analysis` 整节被 Pydantic 静默丢弃 | 配置完全不生效且无告警 | 同上 | ⏸️ |
| A10 | God function/component（chat.py 270 行、Job.tsx 1512 行、bootstrap 220 行） | 维护成本高 | 拆分 | ⏸️ |
| A11 | `_dispatch_tool` 长 if/elif 链 | 违背开闭原则 | 注册表模式 | ⏸️ |
| A12 | 前端全仓 99 处 `any` | 类型契约脱节 | 增量治理 `noImplicitAny` | ⏸️ |
| A13 | 函数体内延迟 import 散布 | 可维护性 | 收敛 | ⏸️ |
| A14 | browser-service 用 print/traceback 代替 logging | 可维护性 | 统一 logging | ⏸️ |
| A15 | 全链路零 trace（collector 未被消费） | 五 Agent 耗时不可知 | 持有 collector + 中间件注入 trace_id | ⏸️ |
| A16 | news/job 两子系统零 metrics 零 trace | 失败/变慢不可见 | 补 `news_*`/`job_*` 指标 | ⏸️ |
| A17 | 4 处 `except: pass` 埋点失败静默 | 监控盲点 | `safe_record()` + 错误计数 | ⏸️ |
| A18 | metrics 标签失真（user_id 恒 default、tool 名硬编码、`clear()` 并发互擦） | 指标不可信 | 修标签 | ⏸️ |
| A19 | 非 chat 路由日志缺 trace_id/user_id | 排查困难 | 中间件统一 `bind_context` | ⏸️ |

## 六、文档（27）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| DOC1 | `.env.example` 只覆盖 4/20 环境变量（缺 `ALLOWED_EMAILS` 等 16 个） | 照它部署 → 功能凭空消失 | 按分组补齐 + 逐项注释后果 | 🔧 |
| DOC2 | `config.yaml:7` 头注释死链（指向已迁移路径） | 第一个配置文件即误导 | 改指 `docs/tech/06-CONFIG-REFERENCE.md` | 🔧 |
| DOC3 | `auth.py` 文档串误写「SHA-256 + 盐」 | 注释与实现漂移 | 改为 PBKDF2-HMAC-SHA256(100k) | 🔧 |
| DOC4 | ops 手册编号写「00-10」，实际已到 15（两处导航） | 低估文档覆盖面 | 统一改 00-15 | 🔧 |
| DOC5 | `prompt/README.md` 目录树失真（列不存在文件、漏 6 个真实文件） | 无法掌握提示词全貌 | 对齐真实文件集 | 🔧 |
| DOC6 | `06-CONFIG-REFERENCE` 自称「全字段字典」仍漏 7 个 env | 「全字段」名不副实 | 补全 + 指向 `.env.example` | 🔧 |
| DOC7 | 7 处「限流未挂载/未生效」过期表述 | 误导排查（本会话曾因此误判 429 成因） | 统一改为「已启用（读写分离）」 | ✅ |
| DOC8 | 根 README「项目结构」整段陈旧（6+ 死链） | 新读者第一步即 404 | 重写或改为链接文档地图 | ⏸️ |
| DOC9 | 根 README 停在 Phase 1-4，未提 JobCopilot/资讯/分享/MCP | 门面与能力脱节 | 以 `tech/00` 口径重构 | ⏸️ |
| DOC10 | 根 README 内嵌 API 列表 + 项目结构，与 tech 文档重复 | 双份维护必然漂移 | README 只导览不复制 | ⏸️ |
| DOC11 | 校验脚本未接入 pre-commit（freshness/links） | 宣称的「自动告警」不存在 | 接入 pre-commit/CI 或如实降级措辞 | ⏸️ |
| DOC12 | `check-doc-sync.sh` 默认恒 `exit 0` | 改代码不同步文档照样通过 → 漂移累积 | 高价值映射改阻断 | ⏸️ |
| DOC13 | `check-links.sh` 只扫 `docs/tech/0*.md` | 「✅ 通过」是假绿 | 扩大扫描范围至全仓 | ⏸️ |
| DOC14 | 四份部署手册重叠（约 3776 行） | 改一处要改四处 | 01 为主 + 平台差异附录 | ⏸️ |
| DOC15 | 漂移清单 12 条 / 待确认 10 条无 owner/期限 | 只登记不消化（D-07 拖 6 天） | 加 owner + 期限 + 状态 | ⏸️ |
| DOC16 | 三份元文档 `based-on-commit` 各不相同 | 无法判断文档对齐基线 | 统一策略或脚本自动写入 | ⏸️ |
| DOC17 | `docs/README.md` 是唯一无 front-matter 的导航入口 | 最重要的入口不受治理 | 补 front-matter | ✅ |
| DOC18 | Phase 5 口径缺失，但 BACKLOG 称已加「Phase 5 能力速览」 | 声称已做但实际不存在 | 补定义或更正 BACKLOG | ✅ |
| DOC19 | 「完整版 12 篇」口径含糊 | 读者困惑 | 统一为「00–11 共 12 篇 + 附录」 | ✅ |
| DOC20 | codeReview 同一天两套日期目录命名并存 | 同批审查被割裂 | 统一 `YYYY-MM-DD` | ✅ 已并入 `2026-09-15/` |
| DOC21 | `RAG常见问题汇总.md` 根目录孤儿、未与实现核对 | 发现不了且内容泛化 | 删（内容通用、无 SEKB 特有信息，git 可追） | ✅ 已删 |
| DOC22 | `tech/{12-*,漂移清单,待确认项清单}` 不匹配校验 glob | 治理盲区 | 纳入地图 + 改 glob | ⏸️ |
| DOC23 | `CHANGELOG.md` 1884 行单文件巨型化 | 可读性 + 冲突风险 | 按季度分卷 | ⏸️ |
| DOC24 | 使用者文档完全缺位（tech 15 + ops 16，用户手册 0） | 新手无手册可读 | 新增 `docs/user/` | ⏸️ |
| DOC25 | 文档与代码 14 处已确认冲突 | 文档不可信 | 逐个校对修复 | ⏸️（部分已修） |
| DOC26 | Alertmanager 邮件 receiver 无 MTA 必失败 | 等于没有邮件通道 | 配真实 SMTP 或删除 | ⏸️ |
| DOC27 | README API 示例缺 `Authorization` 头 | 照抄必 401 | 补 token 获取步骤或指向 `/docs` | ⏸️ |

## 七、使用体验（37）

| # | 问题描述 | 影响 | 建议方案 | 状态 |
|---|---------|------|---------|------|
| U1 | 预览账号点受限功能被**静默跳回首页** | 新手以为系统坏了，直接流失 | 明确「需完整权限」提示 + 顶部横幅 | ✅ |
| U2 | 受限菜单直接隐藏（而非置灰） | 用户以为功能不存在 | 置灰 + 锁图标 + 说明 | ⏸️ |
| U3 | 对话/文件/知识/职位页空态无引导 | 不知第一步点哪 | 空态给说明 + 主操作按钮 | ⏸️ |
| U4 | 首页「快速上手」是死的（`current={-1}` 永不高亮） | 不反映真实进度 | 任务清单式 + 去完成按钮 | ⏸️ |
| U5 | 分享对无账号者打不开（强制登录） | 分享价值腰斩 | 公开只读选项 / 友好落地页 | ✅ |
| U6 | 对话无点赞/点踩（后端已有 rate 接口未接） | 自迭代反馈闭环不可用 | 接上 `rate` 接口 | ✅ |
| U7 | 会话列表无搜索/分组/归档 | 会话多了找不到 | 加搜索 + 分组 | ⏸️ |
| U8 | 知识条目内容被截断到 200 字且无详情入口 | 无法确认/复习 | 点击弹详情抽屉 | ⏸️ |
| U9 | 知识库不能手动新增条目 | 心智模型落空 | 加「手动新增」入口 | ⏸️ |
| U10 | 分类树默认全折叠 | 感知不到「自动三级分类」卖点 | 默认展开一级 | ⏸️ |
| U11 | 文件页把后端环境变量名（`KNOWLEDGE_BASE_ENABLED`）甩给用户 | 普通用户看不懂更慌 | 改友好文案 + 折叠技术详情 | ⏸️ |
| U12 | 「上传历史(临时)」与「已上传文件(持久)」两列表易混 | 分不清 | 合并或明确标注 | ⏸️ |
| U13 | 长任务无进度（周报/月报约 10 分钟、JD 分析 2 分钟） | 以为卡死、反复点击 | 进度条 + 阶段文案 + 耗时预告 | ✅ |
| U14 | 职位分析默认关键词「Agent/北京/50k」是设计者自身条件 | 新用户被误导 | 默认留空 + 引导输入 | ⏸️ |
| U15 | 职位页塞 5 个大 Tab + 「当前搜索」抽象概念 | 认知负荷高 | 引导式流程 | ⏸️ |
| U16 | 投递日期要手打 `YYYY-MM-DD` | 格式错影响冷却期计算 | 改 DatePicker | ⏸️ |
| U17 | BOSS 扫码登录（Cookie/反爬）放在通用设置显眼处 | 干扰主线设置 | 收进「高级设置」 | ⏸️ |
| U18 | 对话分享无集中管理列表（与知识库分享两套机制） | 难以统一心智 | 统一「我的分享」中心 | ⏸️ |
| U19 | 分享创建时无法设有效期，但列表会显示「已过期」 | 用户无法控制分享寿命 | 创建时选有效期 + 到期提醒 | ⏸️ |
| U20 | 失败信息暴露底层异常原文 | 用户看不懂且有泄露风险 | 脱敏为可操作文案 | ⏸️ |
| U21 | CLI 用固定 `default` 用户，与网页数据是两个世界 | 开发者困惑 | 支持 `--user`/说明隔离 | ⏸️ |
| U22 | 三栏固定宽布局，移动端不可用 | 碎片时间无法使用 | 侧栏改抽屉 + ≤768px 适配 | ⏸️ |
| U23 | 破坏性操作确认过简（只说「确定删除？」） | 误删不可恢复 | 说明后果 + 撤销 | ⏸️ |
| U24 | 无统一「任务/通知中心」 | 异步结果只能手动刷新 | 顶栏通知铃铛聚合 | ⏸️ |
| U25 | 整个产品无反馈入口 | 用户声音无处表达 | 右下角反馈面板 | ⏸️ |
| U26 | 无账户删除 / 数据导出 | 合规风险（个保法/GDPR） | 「数据与隐私」卡片 | ⏸️ |
| U27 | 画像系统后台自动抽取，前端完全无入口 | 信任崩塌 | 「我的画像」可见可改可关闭 | ⏸️ |
| U28 | 登录无「活跃会话/踢下线」 | 安全感缺失 | 会话管理面板 | ⏸️ |
| U29 | 429 提示只说「请求过于频繁」不说等多久 | 只能反复刷新 | 倒计时 + 自动重试 | ✅ |
| U30 | 异步上传立即返回 `status=success`，无任务 ID | 后台失败用户不知情 | 返回 task_id + 三态 | ⏸️ |
| U31 | 分析结果「新鲜度」不显式（可能基于旧画像/列表） | 结果可信度存疑 | 显示生成时间 + 覆盖率 | ⏸️ |
| U32 | 无「重新分析」按钮（传 jobs 时跳过缓存直接重算） | 重复花钱却无提示 | 区分「查看(缓存)/重新分析(耗额度)」 | ⏸️ |
| U33 | 搜索历史不可命名/删除/对比 | 多次相似搜索分不清 | 重命名 + 删除 + 对比 | ⏸️ |
| U34 | 多来源抓取部分失败时语义不清 | 不知为何 0 结果 | 分来源展示成功/失败/原因 | ⏸️ |
| U35 | 反馈「踩」3 次即物理删除知识条目，无回收站 | 手滑即不可恢复 | 软删除 + 回收站 + 确认 | ⏸️ |
| U36 | 资讯源固定，用户不可查看/增减/调权 | 内容控制力为零 | 资讯源管理 | ⏸️ |
| U37 | 注册成功后跳回登录页（后端已返回 token） | 多一步手动登录 | 直接登录跳首页 | ⏸️ |

---

## 状态汇总

| 状态 | 数量 | 说明 |
|------|:----:|------|
| ✅ 已修复 | 40 | 前几轮 18 + 本轮范围 1/2 修复 19 + 文档工程 3（DOC17/20/21） |
| 🔧 本轮新增修复 | 10 | 前 8 项 + 本轮部分完成 2 项（S11 监控、S13 分享） |
| ⏸️ 暂缓（待决策） | 101 | 范围 2 剩余 6 项 + 范围 3 排期 + 范围 4 不做 |
| ⛔ 不需修复 | 9 | 已修（报告按旧代码误判）/ 单 worker 下不适用 |
| **合计** | **160** | |

> 状态已按 **2026-09-16 范围 1/2 修复后**更新；清单与理由见同目录 `09-修复状态确认清单.md`。
> 剩余暂缓项 104 条 = 范围 2 剩余 6 项 + 范围 3 排期 20 项 + 范围 4 明确不做项。

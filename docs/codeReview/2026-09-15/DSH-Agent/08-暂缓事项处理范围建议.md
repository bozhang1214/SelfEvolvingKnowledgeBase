# 暂缓事项处理范围建议

> 汇总：DSH-Agent ｜ 日期：2026-09-16
> 输入：`07-全问题总表.md` 中 125 条 ⏸️ 暂缓项
> 判断基准：当前部署形态为**单 worker + 个人/小团队使用 + 监控走 Tailscale**，
> 因此「多 worker 一致性」「高并发优化」「大规模重构」类收益低；优先做**安全性、用户可感知的体验、以及会持续劣化的问题**。

---

## 范围 0 · 已修复，待部署（26 项）

`01`~`07` 报告里 ✅ / 🔧 的全部项（16 代码 + 7 文档 + 1 体验 + 本轮 8 项）。**建议随下一次部署一起上线**。

---

## 范围 1 · 立即做（建议与本次部署一起，约 1 人日）

全部是小改动、无外部依赖、收益明确：

| 条目 | 内容 | 工作量 |
|---|---|---|
| S1 | 重置密码加「原密码确认 / 仅限本人」——堵住账号接管链 | 0.5h |
| S24 | 删 `config.yaml` 的 `jwt_secret` 弱默认字面量（消除误导） | 5min |
| S10 | 401 跳转改用 `import.meta.env.BASE_URL`（修生产 404） | 10min |
| S7 | JWT 有效期 90 天 → 7 天（配置 1 行 + 前端文案） | 0.5h |
| C7 | `market` 的 search_id 为空时改为「无缓存重算」（消除跨搜索串档） | 0.5h |
| C8 | `_report_matched` 改覆盖率阈值（≥80%），消除「永远已过期」 | 0.5h |
| C9 | `job_cache` key 加入 `page/limit/source`（修翻页覆盖） | 0.5h |
| U13 | 周报/月报/JD 分析加「约 N 分钟，可离开稍后回看」+ 阶段文案 | 2h |
| U29 | 429 提示加倒计时 | 1h |
| DOC18/19 | 修正 Phase 5、「12 篇」口径不一致 | 0.5h |

> 这 10 项做完整批部署，性价比最高。

---

## 范围 2 · 近期做（1–2 周，需少量设计或前后端联动）

| 条目 | 内容 | 备注 |
|---|---|---|
| S17 | 异常脱敏：全局异常处理器 + 替换 20+ 处 `f"...{e}"` | 系统性，收益大 |
| S13 | 分享默认 7 天过期 + 访问计数 + 分享维度限流 | 安全 + 信任 |
| S11 | 监控栈鉴权（随机密码 + 绑回环） | **需你先告知监控当前访问方式** |
| S12 | browser-service cookie 端点加内部 token + `chmod 600` | 内部约定 |
| S9 | token 改 httpOnly Cookie | 需配 CSRF 防护，前端有改动 |
| S16 | CORS 改为显式白名单 | 需确认前端域名 |
| U6 | 对话加点赞/点踩（接已有 `rate` 接口） | 后端已就绪，改动小 |
| U5 | 分享支持「公开只读」选项 | **需你定安全边界** |
| U3 | 各页空态引导（Empty State + 主操作） | 首用转化 |
| U30 | 上传返回 `task_id` + 三态（处理中/就绪/失败） | 消除假成功 |
| D8+D9 | `job_cache` LRU 上限 + 资讯清理一并删 json | 止血膨胀 |
| A8+A9 | 配置加 `extra="forbid"`，一次性根治「死配置/配置被静默丢弃」 | 防未来漂移 |
| C5 | `analysis_cache` key 纳入画像版本 | 结果正确性 |
| U27 | 画像透明化（「我的画像」可见/可改/可关） | 信任基础 |
| U28 | 活跃会话管理（列表 + 全部下线） | 安全感 |

---

## 范围 3 · 排期（本季度或**按触发条件**）

| 条目 | 内容 | 触发条件 |
|---|---|---|
| S8 + P5 | jti 黑名单、L1 记忆迁 Redis | **决定上多 worker** 或需要会话跨重启 |
| S15 | PBKDF2 迭代 100k → 600k（登录时惰性重哈希） | 安全审计要求 |
| A1 | 三套 job 缓存统一 `AtomicJsonStore` | 缓存继续膨胀时 |
| P4 + C14 | `stats.records` 环形缓冲 + 流式 token 统计 | 长跑内存/成本准确性问题显现 |
| P8 | 多处同步 IO 改 `asyncio.to_thread` | 出现「一个慢请求卡住全站」 |
| P17+P18 | 资讯逐类并发 + 分类分批 | 日报条目上量、生成超时 |
| A15+A16+A19 | trace 打通 + news/job 指标 | 需要定位线上问题时 |
| DOC8+9+DOC24 | 根 README 重构 + 建 `docs/user/` 用户手册 | 有外部使用者时 |
| DOC11~13 | 治理脚本接入 pre-commit/CI + 扩大扫描 | 文档漂移继续累积时 |
| DOC14 | 四份部署手册合并 | 下次大改部署流程时 |
| U22 | 移动端响应式 | 你有移动端使用需求时 |
| U26 | 数据导出 / 账号注销 | 合规要求或多人使用 |
| U24 | 通知中心 | 异步任务变多时 |
| S21 | prompt injection 清洗策略 | 知识库开始收录外部内容 |

---

## 范围 4 · 不建议做（收益低或与当前部署形态不符）

| 条目 | 不建议做的理由 |
|---|---|
| S19 Gitea 端口绑 0.0.0.0 | 已有 ufw + Tailscale 两层，改动收益低 |
| S20 审计 XFF 取值 | 单层 nginx 下取值已一致，无实际风险 |
| S22 `backup.sh` 明文密钥 | 若确认已废弃 → **直接删脚本**（比修更彻底） |
| S23 邮箱枚举 | 个人项目，探测价值低 |
| D7 `json_storage` tmp 固定名 | 单 worker 部署，无跨进程场景 |
| A7 多 worker 一致性问题 | 不扩容则永不会触发（扩容时随范围 3 一起做） |
| A10/A11 God function/`_dispatch_tool` 重构 | 纯重构、无功能收益，除非要频繁改动这两处 |
| A12 前端 99 处 `any` | 长期治理项，非当前瓶颈 |
| A13/A14 延迟 import / print 代替 logging | 可维护性小项，随手改即可 |
| P12/P13 前端虚拟滚动 / 代码分割 | 当前数据量与访问量都小，收益不明显 |
| P15/P16 Redis pipeline / 懒加载锁 | 微优化，P95 无感知 |
| C12 前后端哈希口径 | 除非真要用「同名同内容跳过」去重 |
| U2 受限菜单置灰 | U1（明确提示）已覆盖主要场景 |
| U17/U21/U33/U34/U36/U37 | 体验细节，建议按你自己实际使用感受再挑 |

---

## 需你先决策的 3 件事（会影响上面的范围）

1. **监控怎么访问的？**（Tailscale? SSH 隧道? 公网端口?）→ 决定 S11 改法，避免改完你自己进不去。
2. **近期会上多 worker 吗？** → 决定 S8/P5/A7 是否现在做；不上就整体推迟。
3. **分享是否允许「无账号公开只读」？** → 决定 U5 的安全边界。

---

## 一句话建议

**先做范围 1（10 项，约 1 人日）与已修复项一起部署**，再把**范围 2**作为下一个迭代；
范围 3 按触发条件排期，**范围 4 建议直接不做**（其中 `backup.sh` 直接删除即可）。

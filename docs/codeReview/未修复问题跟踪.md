# 代码审查问题跟踪表

> 基于 CodeBuddy 与 Qoder 两份审查报告，结合人工全量 review 整理。
> 审查日期：2026-08-13
> 审查范围：backend/app 全模块
>
> **列说明**：
> - `提出时间`：问题首次被记录的日期
> - `进度`：当前处理状态（⏸️ 暂缓 / ⬜ 待处理 / 🔄 进行中；已修复项见"一、已修复问题"）
> - `解决时间`：修复完成日期，未修复留空

---

## 一、已修复问题

| 编号 | 严重度 | 问题摘要 | 提出时间 | 解决时间 | 修复方式 | 验证测试 |
|------|--------|----------|----------|----------|----------|----------|
| P0-1 | 🔴 P0 | LLM 统计全局污染：跨会话/跨用例 token 与成本累加，Scribe 读全局 stats | 2026-08-13 | 2026-08-13 | `LLMFactory.snapshot_stats()` + `delta_stats(snapshot)` 实现 per-request 快照差值；`GraphState.llm_stats_snapshot` 字段由外层注入 | `TestPerRequestStatsIsolation`（5 项） |
| P0-2 | 🔴 P0 | LangGraph 工作流无全局异常兜底：Agent 抛错即 500/中断 | 2026-08-13 | 2026-08-13 | CLI `send()` 与 API `_run_chat()` 均加 try/except 包裹 `graph.ainvoke`，返回降级回复 | `TestWorkflowExceptionFallback`（2 项） |
| P0-3 | 🔴 P0 | LLM 重试覆盖面不足：仅超时/限流重试，5xx/连接错误/402 未覆盖 | 2026-08-13 | 2026-08-13 | 新增 `LLMRetryableError`，`_call()` 中对 5xx/连接重置/402 抛出可重试异常 | `TestLLMRetryAndResponseInit`（8 项） |
| P0-4 | 🔴 P0 | `MemoryError` 命名冲突：自定义异常遮蔽 Python 内置 `MemoryError` | 2026-08-13 | 2026-08-13 | 重命名为 `SEKBMemoError`，全模块引用同步更新 | `TestSEKBMemoErrorNaming`（3 项） |
| P0-5 | 🔴 P0 | `ainvoke_with_stats` 中 `response` 变量在 `finally` 块可能未绑定 | 2026-08-13 | 2026-08-13 | 提前初始化 `response: Any = None` | `TestLLMRetryAndResponseInit.test_response_unbound_on_failure` |
| P0-6 | 🔴 P0 | `e2e_latency_ms` 始终为 0：Scribe 占位但无外层回填 | 2026-08-13 | 2026-08-13 | CLI/API 在 `graph.ainvoke` 后回填 `metrics["e2e_latency_ms"] = latency_ms` | `TestE2ELatencyBackfill`（1 项） |
| 附加-1 | — | LLM 重试次数未追踪：tenacity `nonlocal` 用法错误 | 2026-08-13 | 2026-08-13 | 改用 `before_sleep` 回调 + 可变计数器 | `TestLLMRetryAndResponseInit.test_retry_count_tracked_on_success_after_retry` |
| 附加-2 | — | 降级后成本按原模型计价 | 2026-08-13 | 2026-08-13 | 使用 `actual_model` 而非配置模型计算 cost | `TestLLMRetryAndResponseInit.test_degraded_model_cost_uses_actual_model` |
| 附加-3 | — | `builder.py` 死代码：`chat_simple_node`/`clarify_node` 中 `llm_factory.get()` 返回值未使用 | 2026-08-13 | 2026-08-13 | 删除无用 `llm = llm_factory.get(...)` 调用 | `TestDeadCodeCleanup`（2 项） |
| 附加-4 | — | `config.py` api_key 校验运算符优先级不直观 | 2026-08-13 | 2026-08-13 | 显式加括号 `(not v) or (...)` | `TestApiKeyValidator`（4 项） |
| 附加-5 | — | `server.py` 模块导入即 `create_app()`：缺 config.yaml 时导入失败 | 2026-08-13 | 2026-08-13 | 延迟创建：`get_app()` + `__getattr__` | `TestCreateAppLazy`（3 项） |
| 附加-6 | — | 日志/指标未体现重试/降级 | 2026-08-13 | 2026-08-13 | `LLMCallRecord` 增加 `retried`/`retry_count`/`degraded` 字段；`delta_stats` 输出 `llm_retried` 等；CLI `_print_meta` 展示；`_on_retry` 回调 WARNING 日志 | `TestRetryDegradationVisibility`（3 项） |
| NEW-A | 🟡 P2 | `supervisor.py`/`planner.py` 直接 `float()` 转换 LLM 输出，非数值（如 "高"/null）会抛异常被外层降级，与 `_safe_float` 模式不一致 | 2026-08-13 | 2026-08-13 | 改用 `try/except (TypeError, ValueError)` 安全转换，归一化到 [0,1] | `TestSafeFloatConversion`（3 项） |
| NEW-B | 🟡 P2 | `short_term.compress_if_needed` 取 `_summaries` 列表引用后立即 `append`，若随后 `_merge_summaries` 抛异常，`_summaries` 已部分突变而 `_messages` 未更新，造成状态不一致 | 2026-08-13 | 2026-08-13 | 改为在本地副本 `list(...)` 上操作，所有 LLM 调用成功后再原子提交 | `TestCompressAtomicity`（1 项） |
| NEW-F | 🟡 P2 | 持久化的 `assistant_msg`（CLI+API）仅含 token/延迟，缺重试/降级字段，影响可追溯性 | 2026-08-13 | 2026-08-13 | `assistant_msg` 增加 `llm_retried`/`llm_retry_count`/`llm_degraded`/`llm_degradation_count` | `TestPersistedRetryDegradation`（1 项） |
| SHARE-0 | 🟠 P1 | 分享问答是"伪流式"：先完整 `ainvoke` 再逐字回放，首 token 延迟 = 全量延迟 | 2026-08-28 | 2026-08-28 | `shared_chat_stream` 改用 `llm.astream` 边生成边推送；新增 `_extract_stream_text` 提取增量文本；`_build_rag_context` 连续编号 | `TestExtractStreamText`（5 项）+ `TestBuildRagContext`（3 项） |

---

## 二、未修复问题跟踪

### 🟠 P1 — 中严重度（影响扩展性/准确性/体验）

| 编号 | 问题摘要 | 影响范围 | 建议处理方式 | 提出时间 | 进度 | 解决时间 | 备注 |
|------|----------|----------|--------------|----------|------|----------|------|
| P1-4 | 多 worker 部署下 L1 记忆不一致（纯内存 dict） | 生产多 worker 部署 | Phase 2 迁移到 Redis；压缩摘要持久化到 storage；恢复时优先加载摘要 | 2026-08-13 | ⏸️ Phase 2 | | 当前单 worker 可用；storage 恢复逻辑已部分实现 |
| P1-6 | "已配置未执行"的占位功能：`kb_strict`/`kb_prefer` 意图、`adaptive`/`sampling` 反思策略、`cost_control` 预算检查均未落地 | 配置误导；kb 意图行为与 web_default 无异 | Phase 2 实现 `rag_retrieve`（ChromaDB）；实现成本预算熔断；实现 adaptive/sampling 真实判定 | 2026-08-13 | ⏸️ Phase 2 | | 需明确：要么实现，要么关闭配置项 |
| P1-7 | 主聊天 SSE 流式是"伪流式"：先完整跑完 Graph 再逐字符推送 | 用户体验；首 token 延迟 = 全量延迟 | LangGraph `astream` + Executor `streaming=True` 边生成边推送 | 2026-08-13 | ⏸️ Phase 2 | | 需重构 Executor 为流式产出（分享问答路径已修复，见 SHARE-0） |
| P1-8 | clarify 流程无闭环：仅提问一次即结束，无二次意图识别 | 低置信度问题无法二次确认 | 回写 `clarification_question` 到 state；增加"澄清→再次 Supervisor"条件边 | 2026-08-13 | ⏸️ Phase 2 | | 需增加多轮澄清状态机 |

### 🟡 P2 — 低严重度（代码卫生/可维护性）

| 编号 | 问题摘要 | 影响范围 | 建议处理方式 | 提出时间 | 进度 | 解决时间 | 备注 |
|------|----------|----------|--------------|----------|------|----------|------|
| P2-11 | `LocalTraceCollector` 已实现但未接入 `bootstrap` | 链路追踪不可用 | `initialize_app` 中调用 `setup_tracing`，将 collector 存入 `AppContext` | 2026-08-13 | ⬜ 待处理 | | 改动小 |
| P2-12 | `config.yaml` 中部分配置项语义未对齐代码：`model_switch_threshold` 方向、`sample_rate` 未使用 | 配置误导 | 统一配置项命名与消费点；删除或实现 `sample_rate` | 2026-08-13 | ⬜ 待处理 | | 需与 Phase 2 设计对齐 |
| P2-13 | `reflection.model_switch_threshold` 含义模糊：0.7 是"高于才切换"还是"低于才切换" | 模型切换行为不确定 | 代码中明确比较方向并补充注释 | 2026-08-13 | ⬜ 待处理 | | 改动小 |
| P2-14 | CLI 与 API 存在重复逻辑：`_extract_state_dict`、`_restore_history_to_memory` | 维护成本 | 提取到公共模块（如 `graph/state.py` 或 `core/helpers.py`） | 2026-08-13 | ⬜ 待处理 | | 重构项 |
| P2-15 | `ShortTermMemory.compress_if_needed` 无并发保护 | 多并发请求同一会话时压缩可能竞态 | 加 `asyncio.Lock`（按 conv_id 粒度） | 2026-08-13 | ⬜ 待处理 | | Phase 1 单用户影响小 |
| Q-4.3 | `JSONStorage` 写入虽用 `os.replace` 原子替换，但读-改-写非原子：并发 `append_message` 可能丢数据 | 多并发写入同一会话 | 加文件锁（`fcntl.flock`）或改用 SQLite | 2026-08-13 | ⏸️ Phase 2 | | 单用户 CLI 无影响；API 多请求时需关注 |
| Q-4.5 | `_extract_state_dict` 在 CLI 和 API 两处重复定义 | 维护成本 | 提取到 `graph/state.py` 作为工具函数 | 2026-08-13 | ⬜ 待处理 | | 与 P2-14 合并处理 |
| Q-4.6 | `_safe_float` 在 Critic 和 Scribe 中重复定义 | 维护成本 | 提取到 `BaseAgent` 基类 | 2026-08-13 | ⬜ 待处理 | | 重构项 |
| Q-4.7 | `LLMFactory.stats` 的 `records` 列表无并发保护 | 多线程下统计可能不准 | 加 `threading.Lock` 或改用 `collections.deque` + 原子操作 | 2026-08-13 | ⬜ 待处理 | | asyncio 单线程下无影响 |
| Q-4.8 | `get_config` 使用 `lru_cache` 但参数含路径，不同路径不会命中缓存 | 多配置文件场景 | 确认是否需要支持多配置；若不需要可移除路径参数 | 2026-08-13 | ⬜ 待处理 | | 当前单配置无影响 |
| Q-4.10 | `requirements.txt` 可能含未使用的依赖 | 镜像体积 | 用 `pip-audit` 或 `pip-tools` 检查未使用包 | 2026-08-13 | ⬜ 待处理 | | 代码卫生 |
| Q-4.11 | `now_iso()` 在 `storage/base.py` 和 `graph/state.py` 重复定义 | 维护成本 | 提取到 `core/utils.py` | 2026-08-13 | ⬜ 待处理 | | 重构项 |
| Q-4.12 | `builder.py` 的条件路由函数签名与 LangGraph 1.x 不完全匹配 | 升级风险 | 升级 LangGraph 时需验证 `should_continue` 签名 | 2026-08-13 | ⬜ 待处理 | | 当前版本可用 |
| Q-4.13 | 测试覆盖不足：5 个核心 Agent / 工具层 / API 路由 / CLI 均无单元测试 | 回归风险 | 补充对应单元测试 | 2026-08-13 | 🔄 进行中 | | 本轮已补 P0 修复测试；Agent/API 测试待补 |
| NEW-D | API/CLI 从存储恢复历史到短期记忆时连续 `add_message` 未触发 `compress_if_needed`，超大历史会使内存超限 | 内存占用；大历史会话恢复 | 恢复完成后调用一次 `compress_if_needed` | 2026-08-13 | ⬜ 待处理 | | 改动小；滑动窗口已限制工作集，影响有限 |
| NEW-E | `registry.py` 中 `except (MCPError, Exception)` 冗余（MCPError 是 Exception 子类） | 代码可读性 | 简化为 `except Exception` | 2026-08-13 | ⬜ 待处理 | | 无害，纯代码卫生 |
| NEW-G | CLI `/stats` 命令未展示 `retry_rate`/`degradation_rate`（属性已在 `LLMCallStats` 实现） | 质量监控可见性 | `/stats` 表格增加重试率/降级率行 | 2026-08-13 | ⬜ 待处理 | | 改动小 |

### 🟡 Phase 2 Code Review 暂缓问题（2026-08-17）

> 来源：Qoder + KnowledgeBase 两份 Phase 2 审查报告，以下问题经评估可暂缓修复。

| 编号 | 严重度 | 问题摘要 | 影响范围 | 建议处理方式 | 提出时间 | 进度 | 解决时间 | 备注 |
|------|--------|----------|----------|--------------|----------|------|----------|------|
| P2-P2-01 | 🟡 P2 | 文件上传一次性读入内存（`await file.read()`），50MB 文件先占满内存 | 生产 DoS 风险 | 流式读取 + Content-Length 预校验；PDF/docx 畸形文件防护 | 2026-08-17 | ⏸️ 生产前 | | Phase 2 开发阶段单用户可接受 |
| P2-P2-02 | 🟡 P2 | 文件入库逐块 `add`，未使用 `add_batch` 批量入库 | 性能（大文件入库慢） | `DirectVectorStore` 增加 `add_batch`；`_ingest_chunks` 改用批量接口 | 2026-08-17 | ⬜ 待处理 | | 功能正确，性能优化项 |
| P2-P2-03 | 🟡 P2 | RAG 上下文格式化逻辑重复 3 处（retriever/executor/dispatch_tool） | 代码维护 | 统一为 `format_rag_context` 一处实现，executor 复用 | 2026-08-17 | ⬜ 待处理 | | 格式略有差异，需先设计统一规范 |
| P2-P2-04 | 🟡 P2 | Embedding 模型加载失败后永久降级为哈希向量，无恢复机制 | 生产检索质量崩塌 | 区分"未安装"与"加载失败"；加载失败支持重试；加 `threading.Lock` | 2026-08-17 | ✅ 已修复 | 2026-09-01 | 已改严格模式 `allow_hash_fallback=false`（加载失败抛异常），并重建 collection 消除维度混用，见 ISSUE-010 |
| P2-P2-05 | 🟡 P2 | `tools.vector_store.enabled` 是死配置，bootstrap 只检查 `l3_knowledge.enabled` | 配置误导 | 删除 `tools.vector_store.enabled` 或 bootstrap 同时检查两者 | 2026-08-17 | ⬜ 待处理 | | 不影响功能，配置清理项 |
| P2-P2-06 | 🟡 P2 | `count(user_id)` 用 `collection.get` 加载全部条目后 `len()` | 万级条目时 `/status` 接口慢 | 用 `include=[]` 只取 ids；或维护计数缓存 | 2026-08-17 | ⬜ 待处理 | | 当前数据量小无影响 |
| P2-P2-07 | 🟡 P2 | `from_chroma_record` 的 `metadata` 参数名与类字段同名（shadowing） | 代码可读性 | 参数改名 `raw_meta` 或 `chroma_meta` | 2026-08-17 | ⬜ 待处理 | | 不影响正确性 |
| P2-P2-08 | 🟡 P2 | 后台任务 `_background_ingest` 持有 `AppContext`，`/shutdown` 后可能访问已释放资源 | 后台任务异常 | 改用闭包捕获所需依赖；或独立队列/worker | 2026-08-17 | ⏸️ 生产前 | | Phase 2 无 `/shutdown` 场景 |
| P2-P2-09 | 🟡 P2 | `upload.py` 异常处理将内部错误细节返回客户端（信息泄露） | 安全 | 统一错误响应规范，内部错误返回通用提示 | 2026-08-17 | ⏸️ Phase 3 | | 安全加固项 |
| P2-P2-10 | 🟡 P2 | 冲突检测阈值（0.95/0.80）未用真实 embedding 验证 | 可能误合并/误并存 | 用 bge-small-zh 对"同义/补充/无关"三类文本实测 distance 分布校准 | 2026-08-17 | ⏸️ 生产前 | | 需安装 sentence-transformers 后验证 |
| P2-P2-11 | 🟡 P2 | `KnowledgeBaseBackend` 抽象接口与实现漂移：`retrieve` 缺 `min_score`；`find_similar` 未声明 | 接口契约不一致 | 接口增加 `min_score`/`find_similar`；消除 `getattr` hack | 2026-08-17 | ✅ 已修复 | 2026-08-28 | 已补 `list_entries`/`update`/`count_entries` 进抽象接口，移除 `reclassify_entry` 的 `getattr` hack |
| P2-P2-12 | 🟡 P2 | `knowledge_ingestor.py` 的 `user_input`/`answer` 直接插入 Prompt，存在 prompt injection 风险 | 知识库纯净度 | 对用户输入做清洗/转义 | 2026-08-17 | ⏸️ Phase 3 | | 低概率但影响知识质量 |
| P2-P2-13 | 🟡 P2 | `rag_retrieval_node` 每次请求新建 `RAGRetriever`（无状态对象），重复解析配置 | 微小性能浪费 | 在 `GraphBuilder.__init__` 中缓存实例 | 2026-08-17 | ⬜ 待处理 | | 改动小 |
| P2-P2-14 | 🟡 P2 | `upload.py` 异步处理占位响应 `status="success"` 但 `error="后台异步处理中"`，语义矛盾 | 接口语义混淆 | status 改为 `"processing"` 或 `"accepted"` | 2026-08-17 | ⬜ 待处理 | | 改动小 |
| P2-P2-15 | 🟡 P2 | 事实提取/冲突检测失败直接丢弃知识，无重试/补偿 | 有价值知识可能丢失 | 记录 conv_id 到待重试队列；或离线补采 | 2026-08-17 | ⏸️ Phase 3 | | LLM 内部已有 tenacity 重试 |

### 🟡 分享功能（share）审查新增问题（2026-08-28）

> 来源：在途「知识库分享 + 文档分类」功能 review。

| 编号 | 严重度 | 问题摘要 | 影响范围 | 建议处理方式 | 提出时间 | 进度 | 解决时间 | 备注 |
|------|--------|----------|----------|--------------|----------|------|----------|------|
| SHARE-1 | 🟠 P1 | 分享问答访问者无配额/限流：任何人拿到链接可反复调用 LLM，消耗服务端 DeepSeek 额度 | 成本风险（生产放量后为 P0） | 加分享级/用户级限流或每日配额；对高并发访问降级为摘要式回复 | 2026-08-28 | ⬜ 待处理 | | 分享问答走独立 LLM 路径，不走主聊天 `cost_control` |
| SHARE-2 | 🟡 P2 | 分享问答检索内容 + 用户输入直接拼接进 prompt，存在 prompt injection 风险 | 回答质量/知识纯净度 | 对用户输入做清洗/转义；与 P2-P2-12 统一处理 | 2026-08-28 | ⬜ 待处理 | | 低概率但影响回答可信度 |
| SHARE-3 | 🟡 P2 | `share_storage.append_message` 读-改-写非原子：并发追加同一分享会话可能丢消息 | 数据一致性 | 加文件锁（`fcntl.flock`）或按 (share_id, viewer) 粒度 `asyncio.Lock` | 2026-08-28 | ⬜ 待处理 | | 与 Q-4.3 同类 |

---

## 三、2026-09-09 重构前逐项复核表（WP0）

> 依据《docs/tmp/REFACTORING-PLAN.md》v1.0（已确认全量 M1–M4）执行。结论均带 `file:line` 证据；与旧记录不一致的以本表为准，实际状态在各 WP 落地时回填原表。

| 编号 | 复核结论 | 证据（file:line） | 处置去向 |
|------|----------|-------------------|----------|
| P1-4 多 worker L1 记忆 | ⏸️ 前提仍成立：生产单 worker | `backend/Dockerfile:136-139` `--workers 1` | 不修；11-EVOLUTION 保留（多 worker 上线前） |
| P1-6 "已配置未执行"占位 | ✅ 已关删（D3）：adaptive/sampling 反思 + cost_control 预算字段（无消费）删除；kb_strict/kb_prefer keywords 有消费（supervisor 注入）保留 | config.py/06 文档 | ✅ `3396129` |
| P1-7 主聊天伪流式 | ⚠️ **过期**：2026-09 聊天改造已实现真流式（`astream_events` v2 逐 token + node 事件） | `api/routes/chat.py:407` `_run_chat` | 标记已解决；对应 tracker 状态更新于 WP3 收尾 |
| P1-8 clarify 无闭环 | ✅ 仍成立：clarify → scribe 直连，无二次意图回路 | `graph/builder.py:331` | 保留 11-EVOLUTION（需澄清状态机，功能项非本轮） |
| P2-11 LocalTraceCollector | 🟡 部分过期：`setup_tracing` 已在 bootstrap 调用，但返回值丢弃、collector **无任何消费方** | `core/bootstrap.py:143`；全仓 grep 无消费 | D6=②裁剪 local_json 分支 → WP6 |
| P2-12 `sample_rate` 未使用 | ✅ 已删除（TracingConfig.sample_rate 字段 + config.yaml 键） | `core/config.py`、`config.yaml` | ✅ `ba0c8b4` |
| P2-12/13 `model_switch_threshold` 方向 | ✅ 已澄清（`>=` 阈值→reasoner，config 补 description）；`sample_rate` 无消费→WP6 删 | `core/config.py:156`、`agents/critic.py:63` | ✅ `f178bc3` |
| P2-14 / Q-4.5 `_extract_state_dict` ×3 | ✅ 成立 | `api/routes/chat.py:128`、`cli/chat.py:569`、`eval/runner.py:373` | WP1 收口 `core/utils.py` |
| P2-15 压缩无并发锁 | ✅ 已修复：compress_if_needed 加 conv 粒度 asyncio.Lock（抽 _compress_impl） | `memory/short_term.py` | ✅ `f178bc3` |
| Q-4.3 / SHARE-3 JSON 读改写非原子 | ⚠️ **过期**：JSONStorage 已有 `self._lock`（json_storage.py:77/356） | `storage/json_storage.py` | 已修复，无需改动 |
| Q-4.6 `_safe_float` ×2 | ✅ 成立 | `agents/critic.py:182`、`agents/scribe.py:222` | WP1 |
| Q-4.7 stats 无并发保护 | ⚠️ **过期**：`_record_call` 已有 `async with self._lock`（llm_factory.py:150/538） | `core/llm_factory.py` | 已修复，无需改动 |
| Q-4.8 `get_config` lru_cache+路径参 | ✅ 成立（当前单配置无影响） | `core/config.py` | WP6 固定路径语义 |
| Q-4.10 未用依赖 | ⏳ 待跑工具复核 | `backend/requirements.txt` | WP6 |
| Q-4.11 `now_iso` ×2 | ✅ 成立 | `graph/state.py:245`、`storage/base.py:24` | WP1 |
| Q-4.12 builder 条件路由签名 | ✅ 当前版本可用（升级风险） | `graph/builder.py:319/346` | WP5 固定+版本注释 |
| Q-4.13 Agent/API 测试不足 | 🔄 进行中 → 重构相关模块先垫底 | — | WP0 垫底测试 |
| NEW-D 恢复历史不触发压缩 | ✅ 已修复：API/CLI 恢复后各调用一次 compress_if_needed | `cli/chat.py`、`api/routes/chat.py` | ✅ `f178bc3` |
| NEW-E `except (MCPError, Exception)` | ✅ 成立 | `tools/registry.py` | WP1 |
| NEW-G CLI `/stats` 缺 retry/degradation 率 | ✅ 成立 | `cli/chat.py` `/stats` | WP4 顺带 |
| P2-P2-02 add_batch | ⏸️ 性能项，保留 | `tools/direct/vector_store.py` | 11-EVOLUTION（不在本轮） |
| P2-P2-03 RAG 上下文格式化 ×3 | ✅ 成立 | retriever/executor/`share.py:120` | WP1 统一 `tools/rag/format.py` |
| P2-P2-05 `tools.vector_store.enabled` 死键 | ✅ 已删除（VectorStoreConfig.enabled 字段 + config.yaml 键） | `core/config.py`、`config.yaml` | ✅ `ba0c8b4` |
| SHARE-1 分享无限流 | ✅ 成立：分享问答独立 LLM 路径 | `api/routes/share.py:498` | WP4 通用限流启用后复核 |
| SHARE-2 prompt injection | ✅ 保留（需清洗策略设计） | `api/routes/share.py` 拼接 | 11-EVOLUTION |
| 死指标 conversations_total 等 | ⚠️ 代码 grep 无匹配 → 名称已不存在或已改；以 metrics.py 实测清单为准 | 全仓 grep 空 | WP4 指标清单复核（对齐 09/T8） |
| 主聊天"技能"入口 | ✅ 前端已删按钮；后端 `ChatRequest.skill` 仍在（D2=删除） | `chat.py:106/435-439/546`、`_build_skill_context:210` | WP3 删除（`profile.skills` 简历属性语义不同，保留） |

---

## 四、变更记录

| 日期 | 变更内容 |
|------|----------|
| 2026-08-13 | 初始建立；记录 6 个 P0 已修复 + 6 个附加修复 + 14 个未修复问题 |
| 2026-08-13 | 全量 review 补充：新增修复 NEW-A/NEW-B/NEW-F（+5 测试，共 35 项）；新增未修复 NEW-D/NEW-E/NEW-G；全量测试 239 passed |
| 2026-08-13 | 误操作恢复：全部源码修复与测试文件被撤销后完整重建；全量测试验证通过 |
| 2026-08-17 | Phase 2 新增修复 P0-1~P0-6 + P1-7~P1-11（共 11 项）；新增 Phase 2 暂缓问题 15 项；全量测试 401 passed |
| 2026-08-28 | 格式重构：新增「提出时间 / 进度 / 解决时间」列，去掉文件名日期前缀；在途「分享 + 分类」功能 review：修复分享问答伪流式（SHARE-0，改用 `llm.astream` 真流式）、接口漂移（P2-P2-11）；新增分享功能未修复问题 SHARE-1~3；全量测试 507 passed |
| 2026-09-01 | 服务器线上调试：修复 created_at str 序列化 500（`_fmt_dt`，见 ISSUE-009）；禁用 embedding 哈希降级（严格模式）+ 重建 ChromaDB collection（见 ISSUE-010，标记 P2-P2-04 已修复）；全量测试 510 passed |
| 2026-09-09 | 重构前复核（WP0）：新增「三、逐项复核表」；P1-7 判定已解决（主聊天真流式）、P2-11 部分过期（setup_tracing 已接但 collector 无消费方）、P1-6/D2/D3 决策落定 |
| 2026-09-09 | 重构 WP1 完成（`fc1e084`）：重复清零——`core/utils.py`（now_iso/fmt_dt/safe_float/to_state_dict）+ `tools/rag/format.py`（三处 RAG 格式化归一）；覆盖 P2-14 / Q-4.5 / Q-4.6 / Q-4.11 / P2-P2-03 / NEW-E；新增测试 39 例，全量 567 passed |
| 2026-09-09 | 重构 WP2/WP3 完成（`daa76bd`）：服务层下沉 5 模块（browser/upload/share/job/profile）+ 删 skill；chat.py 828→538 行 |
| 2026-09-09 | 重构 WP4 完成（`b49889a`/`8f7a2c3`）：LLM 统一入口 9 处收口 + RateLimit 条件挂载；Q-4.7 复核已有锁 |
| 2026-09-09 | 重构 WP5 完成（`f178bc3`）：P2-15 压缩锁 + NEW-D 恢复压缩 + P2-13 语义澄清；Q-4.3 复核已有锁 |
| 2026-09-09 | 重构 WP6（`ba0c8b4`/`3396129`）：死代码（trigger_now/verify_phase3）+ 死配置（sample_rate/vector_store.enabled）+ D3 占位关删（adaptive/sampling/cost_control 预算） |
| 2026-09-15 | 归档 codeReview 原始审查报告：将 2026-09-08 全域评审两轮约 120 项问题合并进「五、2026-09-08 全域评审问题」，删除 11 份原始报告，只保留本跟踪表（原始报告可从 git 历史找回） |

---

## 五、2026-09-08 全域评审问题（合并自已删除的审查报告）

> 来源：`01-工程评审报告.md`（第一轮，90 项）+ `04-第二轮-增量功能与全量复核.md`（R2-01~30 + EN-15~19）。
> 状态说明：**本批问题为 2026-09-08 评审提出，除标注「✅ 已修复」外均按「⬜ 待复核」归档**——
> 后续历经 09-09 重构（WP0–WP6）、JobCopilot P1–P7、MCP、资讯改造等多轮迭代，多数问题
> **可能已修复或被取代**，但未逐项回验。需要用哪一项时，按「证据」列去代码里核实当前状态。
>
> 严重度：**S**=严重（安全/系统性）｜**A**=中等｜**B**=低/工程化。优先级：**P0**=立刻｜**P1**=近期｜**P2**=可后置。

### 安全（SEC）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| SEC-01 | S/P0 | 聊天路由跨用户越权（IDOR）：任意登录用户传他人 `conversation_id` 可读他人历史、写入他人会话 | `get_conversation` 增加 `user_id` 校验或统一 `@require_owner` 装饰器 |
| SEC-02 | S/P0 | JWT：默认硬编码密钥可伪造 token；登出无黑名单；刷新无限续期 | 缺失 `JWT_SECRET` 拒绝启动；`jti`+Redis 黑名单；缩短 access token |
| SEC-03 | S/P0 | 限流中间件整体关闭，`/analyze`（7 次 LLM）/`/refresh`（14~36 次 LLM）无防护 | ✅ **已修复**（09-10 启用分组限流，本会话又做读写分离） |
| SEC-04 | A/P1 | 兜底异常处理器把异常原文直出客户端 | 统一返回脱敏 error_id |
| SEC-05 | A/P1 | `GET /news/reports`、`/report` 无鉴权 | ✅ **已修复**（资讯路由已加鉴权，实测 401） |
| SEC-06 | A/P1 | 资讯 RSS 抓取无 scheme 白名单/私网黑名单/响应大小上限 | URL 限制 http/https + 拒私网 + `max_bytes` 截断 |
| SEC-07 | A/P1 | BOSS 登录 cookie 明文写盘、登录态原样透传前端 | 只返回 `{ok:true}`，cookie 加密服务端存 |
| SEC-08 | A/P1 | token 存 localStorage；401 跳 `/login` 而 basename 是 `/sekb` → 生产 404 | 跳 `${basename}/login`；长期 httpOnly cookie |
| SEC-09 | B/P1 | 零依赖/镜像漏洞扫描（无 gitleaks/pip-audit/npm audit） | 加 gitleaks + pip-audit + npm audit |
| SEC-10 | B/P2 | CSP 含 `unsafe-inline`；备份明文含 `.env.prod` | CSP 去 unsafe-inline；备份加密 |
| SEC-11 | A/P1 | 采集层逆向解密目标站响应、伪装请求头（合规风险） | 法务评估；优先官方 API；保留则加 robots 尊重+限流 |
| SEC-12 | B/P1 | `users.json` 直接覆盖写，非原子无锁 | 复用 tmp+`os.replace` 模式 |

### 并发与数据（DATA）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| DATA-01 | S/P0 | `json_storage` read-modify-write 无锁，并发丢消息 | ✅ **部分修复**（已有 `_lock`；跨进程锁待评估） |
| DATA-02 | A/P1 | 资讯 `index.json` 非原子写，损坏静默返回空 | tmp+rename+锁+告警 |
| DATA-03 | A/P1 | 软删留索引；job 三处缓存只增不清理 → 无限增长 | 删时清理索引；落盘前过滤过期键 |
| DATA-04 | A/P1 | 归档按全局 200 条截断，多用户互挤 + 孤儿文件 | 按 user 分组淘汰 + 同步清理文件 |
| DATA-05 | B/P2 | 资讯清理只删 `daily_*.md`，`.json`/周月报永不清理 | 对齐清理策略 |
| DATA-06 | B/P2 | `storage.postgres` 死配置 | 决策：做或删 |
| DATA-07 | A/P1 | 上传 JD 截图走错解析→乱码进 7 步 LLM | 按扩展名分流 process_file/ImageProcessor |
| DATA-08 | A/P1 | 资讯空报告也落盘，当天后续重试全被幂等短路 | ✅ **已修复**（生成前校验，失败写状态+告警+补跑） |

### 成本与性能（PERF）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| PERF-01 | A/P1 | 日报 14~36 次 LLM 全串行，单次 10~20 分钟 | 逐类 `asyncio.gather` + 信号量 |
| PERF-02 | S/P0 | 分类 prompt 无分批/上限，300 条→超长失败→静默退化关键词分类 | 按 50~80 条分批并发 + 成功率指标 |
| PERF-03 | A/P1 | RSS 约 40 源无连接池/重试/熔断 | 共享 client + Semaphore + 单源超时重试 |
| PERF-04 | A/P1 | 周/月报重抓全量，不复用已落盘日报 | 基于已存结构化日报聚合 |
| PERF-05 | A/P1 | job 分析 7 步无短路，前步失败后续仍跑 | 加短路（依赖 degraded 标记） |
| PERF-06 | A/P1 | job 缓存同步文件 IO 跑在 async 路由 | `asyncio.to_thread` 或 SQLite |
| PERF-07 | B/P2 | 前端无代码分割，首屏 2.05MB 整包 + 死依赖 | React.lazy 拆页 + manualChunks |
| PERF-08 | B/P2 | 会话/文件全量渲染无虚拟滚动 | 虚拟列表 + 分页 |
| PERF-09 | B/P2 | `stats.records` 无界增长 | 环形缓冲 |
| PERF-10 | B/P2 | 资讯十余处魔法数字硬编码 | 迁入 config.yaml |

### 可观测性（OBS）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| OBS-01 | S/P0 | 全链路零 trace：LocalTraceCollector 返回值被丢弃，五个 Agent 耗时不可知 | 持有 collector + 中间件生成透传 trace_id |
| OBS-02 | A/P1 | 多个指标零埋点（llm_call_latency 等） | 补埋点或删指标 |
| OBS-03 | A/P1 | metrics 标签失真（user_id 恒 default）+ `clear()` 并发互擦 | 真实标签 + 删 clear() |
| OBS-04 | A/P1 | `daily_cost_usd` 只增不重置，无重置任务 | 按 UTC 日 label 或定时重置 |
| OBS-05 | A/P1 | 4 处 `except: pass` 埋点失败静默 | 统一 safe_record + 计 metrics_error_total |
| OBS-06 | B/P1 | 非 chat 路由日志缺 trace_id/user_id | 中间件统一 bind_context |
| OBS-07 | A/P1 | news/job 两子系统零 metrics 零 trace | ✅ **部分修复**（本会话加了 news 状态/告警；指标仍缺） |
| OBS-08 | B/P2 | eval 不传 vector_store，评估场景无 RAG | eval 支持注入 vector_store |
| OBS-09 | A/P1 | news 周/月报路径无锁；job 三处缓存无锁 | 统一 storage/lock.py |

### 运维（OPS）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| OPS-01 | S/P1 | 灰度发布链路实际不可用（路径 404/回滚文件不存在） | 修复或标注未启用并下线 |
| OPS-02 | S/P1 | 备份含 `.env.prod`；restore 无对应步骤/非自动化 | 补配置恢复 + `--force`/`--clean` + 演练 |
| OPS-03 | A/P1 | backend 端口 `8000:8000` 暴露，与文档/脚本冲突 | ✅ **已修复**（现 `127.0.0.1:8000:8000`） |
| OPS-04 | A/P1 | Alertmanager 邮件 receiver 无 MTA 必然失败 | 改真实 SMTP 或只留飞书 |
| OPS-05 | A/P1 | 告警高基数 label + 低流量除零 + BackendDown 1m 过敏 | 移除高基数 label、加分母保护、`for:3m` |
| OPS-06 | A/P1 | 无 node-exporter；Loki healthcheck 禁用 | 加 node-exporter + blackbox |
| OPS-07 | A/P1 | CI mypy `|| true`、eval 非阻断 → 门禁虚设 | ✅ **已修复**（本会话 mypy 门禁已 fail-on-error + eval 门禁） |
| OPS-08 | B/P2 | `nginx-ssl.conf` 整份不可用（root 路径/废弃语法/占位域名） | 统一模板或删除 |
| OPS-09 | B/P2 | 前端镜像 nginx root 运行 + HEALTHCHECK 与 301 冲突 | 加 USER + 修正 HEALTHCHECK |
| OPS-10 | B/P2 | `:latest` 标签版本漂移 | 固定 sha 或 IMAGE_TAG 贯穿 |

### 文档（DOC）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| DOC-01 | S/P1 | 根 README 只到 Phase 4，API 表缺 news/job/share 等 | 同步新能力 |
| DOC-02 | S/P1 | docs 导航表漏 8 篇 → 事实孤儿 | 补导航 |
| DOC-03 | A/P1 | 4 份部署手册三节重复 3~4 次 | 合并去重 |
| DOC-04 | A/P1 | PRE-DEPLOY-CHECKLIST 与脚本双份维护必然漂移 | ✅ **部分修复**（本会话修了 cron/Git remote 误报；未合并） |
| DOC-05 | A/P1 | CHANGELOG/BACKLOG/ISSUES/未修复跟踪 四份边界重叠 | 明确各自职责 |
| DOC-06 | A/P1 | 文档与代码间 14 处冲突 | 逐条校对 |
| DOC-07 | B/P2 | 文档缺版本/更新时间元信息 | 补元信息 |
| DOC-08 | B/P2 | RAG 常见问题游离体系外 | 与 RAG-01~08 建立映射 |
| DOC-09 | B/P2 | 架构文档状态「设计评审中」过期 | 更新状态 |
| DOC-10 | B/P2 | 无术语表、ASCII 架构图 | Mermaid 化 + 术语表 |

### Agent 架构（AGENT）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| AGENT-01 | S/P0 | `needs_rewrite` 只落到 scribe 原样返回，反思形同虚设 | ✅ **已修复**（本会话前已加 rewrite 节点） |
| AGENT-02 | A/P1 | Critic 提前 `replan_count+1`，实际重规划 = max-1 | 改由 should_replan 判定后自增 |
| AGENT-03 | A/P1 | `should_reflect` 无调用者，reflection.policy 不生效 | 在 supervisor→planner 条件边接入 |
| AGENT-04 | A/P1 | 无 checkpointer/recursion_limit/超时/恢复 | 显式 recursion_limit + checkpointer |
| AGENT-05 | S/P1 | LLM 重试计数失真（retry_count 只 0/2 两态） | 用 retry_state.attempt_number 回填 |
| AGENT-06 | S/P0 | news/job 主链路裸 `ainvoke` 绕过 stats（成本 0、无重试） | 统一入口禁裸 ainvoke |
| AGENT-07 | A/P0 | 提示词目录硬编码猜测 + 孤儿提示词 + 改 md 需重启 | PromptRegistry + 路径进 config |
| AGENT-08 | A/P1 | `str.replace("{{k}}",v)` 无转义无残留校验，可能反向污染 | string.Template/StrictUndefined |
| AGENT-09 | S/P0 | 五 Agent「异常→假成功」，HTTP 200 掩盖失败（头号系统性风险） | degraded 标记贯穿 + 指标告警 + 话术改「暂时不可用」 |
| AGENT-10 | A/P1 | LLM 输出仅 json.loads 兜底，无 Schema 校验 | 各步 Pydantic Model + 失败重试 |
| AGENT-11 | A/P1 | `hard_token_limit` 死配置，超长输入无硬闸 | 实现硬限 + context_truncated 指标 |
| AGENT-12 | A/P1 | 步骤串行执行，depends_on 无人调度 | 拓扑分层 + asyncio.gather |
| AGENT-13 | B/P1 | 预检索结果不回灌 Supervisor，kb 意图退化关键词匹配 | 预检索前移 supervisor |
| AGENT-14 | B/P1 | Critic issues/suggestions 重规划时不读，盲重试 | Planner 注入 previous_attempt |
| AGENT-15 | A/P1 | cost_control 预算字段死配置，BudgetExceededError 从未 raise | per-request 成本累计+熔断 |
| AGENT-16 | B/P2 | web_search 无缓存 | TTL 缓存 + 命中指标 |

### RAG / 知识库（RAG）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| RAG-01 | S/P0 | 入库评分公式恒定项致几乎全会入库，闸门失效 | 修公式 + 阈值进 config + 决策指标 |
| RAG-02 | A/P1 | l3 eviction 死配置，无过期/版本/清理 | version/valid_until + 淘汰任务 |
| RAG-03 | A/P1 | 纯向量检索，无 BM25/Rerank/Query Rewrite | Query Rewrite → BM25 → Rerank → 元数据过滤 |
| RAG-04 | A/P2 | 固定 500/50 分块，无文档策略/表格保留 | 递归分块 + 表格保留 |
| RAG-05 | B/P2 | 未做繁简归一 | OpenCC 转简 |
| RAG-06 | A/P1 | get/update/delete 无 user_id 参数，隔离靠上层 | 强制 user_id 入参 |
| RAG-07 | B/P2 | count 用 get 全量拉 ID 再计数 | Chroma count API |
| RAG-08 | A/P1 | eval 数据集 10 条，无答案正确性断言 | 扩到 30~50 + faithfulness |
| RAG-09 | B/P1 | 检索异常吞成「知识库为空」 | 区分空/失败（依赖 AGENT-09） |

### 前端（FE）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| FE-01 | S/P0 | store 5 处静默 catch，失败用户零感知 | 统一 {ok,error} + 全局错误提示 |
| FE-02 | A/P1 | SSE 无重连/心跳/超时；controller 存全局变量 | 重连 + Last-Event-ID + store Map |
| FE-03 | B/P2 | Job 页 45 处 any，类型契约脱节 | noImplicitAny 增量治理 |
| FE-04 | B/P2 | vitest include 排除 tsx，零组件测试 | ✅ **已修复**（本会话新增 News 组件测试） |
| FE-05 | B/P2 | 每 token 全量 setState，长对话掉帧 | token 节流合并 |
| FE-06 | B/P2 | 生产仍 console.log 完整 payload；上报端点无鉴权 | 降级 warn+ + 字段白名单 |

### 第二轮（R2-01~30）

| 编号 | 严重度/优先级 | 问题摘要 | 建议 |
|------|------|----------|------|
| R2-01 | S/P0 | 分享分类 `is_scoped` 只判 l1，l2/l3-only 退化全库 | 改 l1 or l2 or l3 |
| R2-02 | S/P0 | 分享链接永不过期无撤销 | 默认 7/30 天 |
| R2-03 | S/P0 | 备份脚本失败即 leaving backend stopped，无 trap/restore | ✅ **部分修复**（backup_kb.sh 本会话加去重/保留；trap 待确认） |
| R2-04 | S/P0 | 向量重建脚本不在镜像内，delete→add 无事务 | COPY scripts + 原子重建 |
| R2-05 | S/P0 | 前端队列永久卡死（onDone 空 conv_id 不 flush） | 补 flushQueue + 停止按钮渲染 |
| R2-06 | S/P0 | 主聊天伪流式 | ✅ **已修复**（09 聊天改造 astream_events 真流式） |
| R2-07 | A/P1 | 会话「锁」set + 新会话键退化 `user_id\|` | 键加 temp:uuid 或 user 级 |
| R2-08 | A/P1 | 非流式 POST 无 inflight 保护 | 复用保护或返回 409 |
| R2-09 | A/P1 | skill 指令拼进 user_input → 注入面 + 二次判意图冲突 | ✅ **部分修复**（skill 已删；注入面见 SEC-11 类） |
| R2-10 | A/P1 | 应聘助手每轮 +1 记录员 LLM，后台任务无上限 | 改对话结束触发 + 信号量 |
| R2-11 | A/P1 | 画像更新不失效职位分析缓存（D14 闭环断裂） | 写入清缓存 + 缓存键加 profile hash |
| R2-12 | A/P1 | 画像 upsert 无锁 + 固定 .tmp 双写互覆盖 | mkstemp + 文件锁 + 版本号 |
| R2-13 | A/P1 | `"data/profile"` 硬编码 5 处 | 装配 AppContext |
| R2-14 | A/P1 | LLM 输出直写画像无字段/长度校验 | 白名单 + 截断 + 值域 |
| R2-15 | A/P1 | 画像/报告同步 IO 阻塞事件循环 | asyncio.to_thread + TTL 缓存 |
| R2-16 | A/P1 | 排队消息不绑 conv_id，onDone 抢回 currentConvId | 队列项存 conv_id |
| R2-17 | A/P1 | 队列无长度上限 | 上限 5 条 + 提示 |
| R2-18 | A/P0 | 已有前端单测被打红（7 参 vs 6 参断言） | ✅ **已修复**（本会话前端测试全绿 68 项） |
| R2-19 | A/P1 | MD5 索引非原子、异步上传不写 MD5 | 带锁原子写 + 补写 + 迁移脚本 |
| R2-20 | A/P1 | upload 5 处 `limit=5000` 硬上限大库截断 | 分页拉全量 |
| R2-21 | B/P2 | skill 名中文字符串硬匹配，无枚举 | 共享枚举 |
| R2-22 | B/P2 | 首 token 打点恒 0，SSE 不走拦截器 | onToken 首回调打点 |
| R2-23 | B/P2 | Drawer/Sider 列表重复、无分页/虚拟滚动 | 抽组件 + 虚拟滚动 |
| R2-24 | B/P2 | `ChatRequest.user_id` 死字段 | 删除 |
| R2-25 | B/P2 | `get_app_context` 未初始化抛 NameError 而非 RuntimeError | 模块级 None + 显式抛 |
| R2-26 | B/P1 | profile/share/队列/skill/series 零测试 | 补并发/过滤/flush 测试 |
| R2-27 | B/P1 | count 失败→total 0→误报「没有内容」 | 区分空/失败 |
| R2-28 | B/P2 | 分类限定未同步 chat_share | 统一分享能力 |
| R2-29 | B/P2 | 重建脚本用 assert，`python -O` 下剥离 | 显式 if raise |
| R2-30 | B/P2 | 每字符 sleep(0)，2000 字 2000 帧 | 随 R2-06 解决 |

### 功能增强建议（EN，非问题，记录备查）

> EN-15 真流式输出（随 R2-06 已落地）｜EN-16 画像版本化+审计+缓存联动｜EN-17 队列产品化｜EN-18 分享能力补齐（l2/l3/过期/撤销）｜EN-19 备份可恢复性工程化。

---

## 六、已排期事项（from 2026-09-15 四方审查的「范围 3」）

> 来源：`docs/codeReview/2026-09-15/DSH-Agent/08-暂缓事项处理范围建议.md` 范围 3。
> 这些**不是当前 bug**，而是**按触发条件排期**的改进；触发条件未出现前不做。

| 编号 | 事项 | 触发条件 | 备注 |
|------|------|----------|------|
| SCH-1 | jti 黑名单 + L1 短期记忆迁 Redis | **决定上多 worker** 或需要会话跨重启 | 多 worker 的前置（另见 SCH-8） |
| SCH-2 | PBKDF2 迭代 100k → 600k（登录时惰性重哈希） | 安全审计要求 | OWASP 建议值 |
| SCH-3 | 三套 job 缓存统一 `AtomicJsonStore`（锁+原子写+LRU） | 缓存继续膨胀时 | 原子写/上限已单点修复，统一是收敛 |
| SCH-4 | `stats.records` 环形缓冲 + 流式 token 成本统计 | 长跑内存/成本准确性问题显现 | 需同时改 per-request delta 机制 |
| SCH-5 | 多处同步 IO 改 `asyncio.to_thread` | 出现「一个慢请求卡住全站」 | 当前单 worker 下偶发 |
| SCH-6 | 资讯逐类并发 + 分类分批 | 日报条目上量、生成超时 | 现在约 10~20 分钟 |
| SCH-7 | trace 打通 + news/job 指标补齐 | 需要定位线上问题时 | 目前两子系统仅有 logger |
| SCH-8 | 多 worker 化（Redis + Chroma server + 调度选主） | **CPU 打满**或并发互相卡顿 | 建议先做 SCH-5 再评估 |
| SCH-9 | 根 README 重构 + 建 `docs/user/` 用户手册 | 有外部使用者时 | README 仍停在 Phase 1-4 |
| SCH-10 | 文档治理脚本接入 pre-commit/CI + 扩大扫描范围 | 文档漂移继续累积时 | 现为「假绿」 |
| SCH-11 | 四份部署手册合并（3776 行） | 下次大改部署流程时 | C5，已挂账 |
| SCH-12 | 移动端响应式 | 你有移动端使用需求时 | 当前三栏固定宽 |
| SCH-13 | 数据导出 / 账号注销 | 合规要求或多人使用 | 个保法/GDPR |
| SCH-14 | 统一通知中心 | 异步任务变多时 | 现靠 toast + 手动刷新 |
| SCH-15 | 知识入库 / 分享的 prompt injection 清洗 | 知识库开始收录外部内容 | 需清洗策略设计 |
| SCH-16 | 上传 task_id + 三态（处理中/就绪/失败） | 异步上传失败率高时 | 现立即返回假 success |
| SCH-17 | token 改 httpOnly Cookie（+CSRF 防护） | XSS 风险需进一步收敛时 | 前端鉴权重构，改动大 |
| SCH-18 | 用户画像透明化（可见/可改/可关闭） | 多人使用 / 信任诉求提升时 | 需新后端端点 + 前端页 |
| SCH-19 | 活跃会话管理（设备列表 / 全部下线） | 多人使用 / 账号安全诉求提升时 | 需会话追踪基础设施 |
| SCH-20 | 配置 `extra="forbid"`（根治死配置） | 下次大改配置时 | 需先清理 config.yaml 中未建模键 |

### 已明确的决策（2026-09-16）

- **监控访问**：确认走 Tailscale（`100.71.24.105`）。已做：feishu-webhook 绑回环 + Grafana 口令加入 env 示例。**未做**：Prometheus/Loki/Alertmanager/Gitea 改绑 Tailscale IP（待确认 Tailscale IP 稳定性）。
- **多 worker**：**不上**。理由：瓶颈是 LLM 网络延迟而非 CPU；代价是 Redis + Chroma server + 调度选主。触发条件：CPU 打满或并发互卡（见 SCH-8）。
- **分享公开只读**：**不允许**。分享必须登录后查看；已把「静默跳登录页」改为明确的登录引导页。

# 代码审查问题跟踪表

> 基于 CodeBuddy 与 Qoder 两份审查报告，结合人工全量 review 整理。
> 审查日期：2026-08-13
> 审查范围：backend/app 全模块

---

## 一、已修复问题

| 编号 | 严重度 | 问题摘要 | 修复方式 | 验证测试 |
|------|--------|----------|----------|----------|
| P0-1 | 🔴 P0 | LLM 统计全局污染：跨会话/跨用例 token 与成本累加，Scribe 读全局 stats | `LLMFactory.snapshot_stats()` + `delta_stats(snapshot)` 实现 per-request 快照差值；`GraphState.llm_stats_snapshot` 字段由外层注入 | `TestPerRequestStatsIsolation`（5 项） |
| P0-2 | 🔴 P0 | LangGraph 工作流无全局异常兜底：Agent 抛错即 500/中断 | CLI `send()` 与 API `_run_chat()` 均加 try/except 包裹 `graph.ainvoke`，返回降级回复 | `TestWorkflowExceptionFallback`（2 项） |
| P0-3 | 🔴 P0 | LLM 重试覆盖面不足：仅超时/限流重试，5xx/连接错误/402 未覆盖 | 新增 `LLMRetryableError`，`_call()` 中对 5xx/连接重置/402 抛出可重试异常 | `TestLLMRetryAndResponseInit`（8 项） |
| P0-4 | 🔴 P0 | `MemoryError` 命名冲突：自定义异常遮蔽 Python 内置 `MemoryError` | 重命名为 `SEKBMemoError`，全模块引用同步更新 | `TestSEKBMemoErrorNaming`（3 项） |
| P0-5 | 🔴 P0 | `ainvoke_with_stats` 中 `response` 变量在 `finally` 块可能未绑定 | 提前初始化 `response: Any = None` | `TestLLMRetryAndResponseInit.test_response_unbound_on_failure` |
| P0-6 | 🔴 P0 | `e2e_latency_ms` 始终为 0：Scribe 占位但无外层回填 | CLI/API 在 `graph.ainvoke` 后回填 `metrics["e2e_latency_ms"] = latency_ms` | `TestE2ELatencyBackfill`（1 项） |
| 附加-1 | — | LLM 重试次数未追踪：tenacity `nonlocal` 用法错误 | 改用 `before_sleep` 回调 + 可变计数器 | `TestLLMRetryAndResponseInit.test_retry_count_tracked_on_success_after_retry` |
| 附加-2 | — | 降级后成本按原模型计价 | 使用 `actual_model` 而非配置模型计算 cost | `TestLLMRetryAndResponseInit.test_degraded_model_cost_uses_actual_model` |
| 附加-3 | — | `builder.py` 死代码：`chat_simple_node`/`clarify_node` 中 `llm_factory.get()` 返回值未使用 | 删除无用 `llm = llm_factory.get(...)` 调用 | `TestDeadCodeCleanup`（2 项） |
| 附加-4 | — | `config.py` api_key 校验运算符优先级不直观 | 显式加括号 `(not v) or (...)` | `TestApiKeyValidator`（4 项） |
| 附加-5 | — | `server.py` 模块导入即 `create_app()`：缺 config.yaml 时导入失败 | 延迟创建：`get_app()` + `__getattr__` | `TestCreateAppLazy`（3 项） |
| 附加-6 | — | 日志/指标未体现重试/降级 | `LLMCallRecord` 增加 `retried`/`retry_count`/`degraded` 字段；`delta_stats` 输出 `llm_retried` 等；CLI `_print_meta` 展示；`_on_retry` 回调 WARNING 日志 | `TestRetryDegradationVisibility`（3 项） |
| NEW-A | 🟡 P2 | `supervisor.py`/`planner.py` 直接 `float()` 转换 LLM 输出，非数值（如 "高"/null）会抛异常被外层降级，与 `_safe_float` 模式不一致 | 改用 `try/except (TypeError, ValueError)` 安全转换，归一化到 [0,1] | `TestSafeFloatConversion`（3 项） |
| NEW-B | 🟡 P2 | `short_term.compress_if_needed` 取 `_summaries` 列表引用后立即 `append`，若随后 `_merge_summaries` 抛异常，`_summaries` 已部分突变而 `_messages` 未更新，造成状态不一致 | 改为在本地副本 `list(...)` 上操作，所有 LLM 调用成功后再原子提交 | `TestCompressAtomicity`（1 项） |
| NEW-F | 🟡 P2 | 持久化的 `assistant_msg`（CLI+API）仅含 token/延迟，缺重试/降级字段，影响可追溯性 | `assistant_msg` 增加 `llm_retried`/`llm_retry_count`/`llm_degraded`/`llm_degradation_count` | `TestPersistedRetryDegradation`（1 项） |

---

## 二、未修复问题跟踪

### 🟠 P1 — 中严重度（影响扩展性/准确性/体验）

| 编号 | 问题摘要 | 影响范围 | 建议处理方式 | 状态 | 备注 |
|------|----------|----------|--------------|------|------|
| P1-4 | 多 worker 部署下 L1 记忆不一致（纯内存 dict） | 生产多 worker 部署 | Phase 2 迁移到 Redis；压缩摘要持久化到 storage；恢复时优先加载摘要 | ⏸️ Phase 2 | 当前单 worker 可用；storage 恢复逻辑已部分实现 |
| P1-6 | "已配置未执行"的占位功能：`kb_strict`/`kb_prefer` 意图、`adaptive`/`sampling` 反思策略、`cost_control` 预算检查均未落地 | 配置误导；kb 意图行为与 web_default 无异 | Phase 2 实现 `rag_retrieve`（ChromaDB）；实现成本预算熔断；实现 adaptive/sampling 真实判定 | ⏸️ Phase 2 | 需明确：要么实现，要么关闭配置项 |
| P1-7 | SSE 流式是"伪流式"：先完整跑完 Graph 再逐字符推送 | 用户体验；首 token 延迟 = 全量延迟 | LangGraph `astream` + Executor `streaming=True` 边生成边推送 | ⏸️ Phase 2 | 需重构 Executor 为流式产出 |
| P1-8 | clarify 流程无闭环：仅提问一次即结束，无二次意图识别 | 低置信度问题无法二次确认 | 回写 `clarification_question` 到 state；增加"澄清→再次 Supervisor"条件边 | ⏸️ Phase 2 | 需增加多轮澄清状态机 |

### 🟡 P2 — 低严重度（代码卫生/可维护性）

| 编号 | 问题摘要 | 影响范围 | 建议处理方式 | 状态 | 备注 |
|------|----------|----------|--------------|------|------|
| P2-11 | `LocalTraceCollector` 已实现但未接入 `bootstrap` | 链路追踪不可用 | `initialize_app` 中调用 `setup_tracing`，将 collector 存入 `AppContext` | ⬜ 待处理 | 改动小 |
| P2-12 | `config.yaml` 中部分配置项语义未对齐代码：`model_switch_threshold` 方向、`sample_rate` 未使用 | 配置误导 | 统一配置项命名与消费点；删除或实现 `sample_rate` | ⬜ 待处理 | 需与 Phase 2 设计对齐 |
| P2-13 | `reflection.model_switch_threshold` 含义模糊：0.7 是"高于才切换"还是"低于才切换" | 模型切换行为不确定 | 代码中明确比较方向并补充注释 | ⬜ 待处理 | 改动小 |
| P2-14 | CLI 与 API 存在重复逻辑：`_extract_state_dict`、`_restore_history_to_memory` | 维护成本 | 提取到公共模块（如 `graph/state.py` 或 `core/helpers.py`） | ⬜ 待处理 | 重构项 |
| P2-15 | `ShortTermMemory.compress_if_needed` 无并发保护 | 多并发请求同一会话时压缩可能竞态 | 加 `asyncio.Lock`（按 conv_id 粒度） | ⬜ 待处理 | Phase 1 单用户影响小 |
| Q-4.3 | `JSONStorage` 写入虽用 `os.replace` 原子替换，但读-改-写非原子：并发 `append_message` 可能丢数据 | 多并发写入同一会话 | 加文件锁（`fcntl.flock`）或改用 SQLite | ⏸️ Phase 2 | 单用户 CLI 无影响；API 多请求时需关注 |
| Q-4.5 | `_extract_state_dict` 在 CLI 和 API 两处重复定义 | 维护成本 | 提取到 `graph/state.py` 作为工具函数 | ⬜ 待处理 | 与 P2-14 合并处理 |
| Q-4.6 | `_safe_float` 在 Critic 和 Scribe 中重复定义 | 维护成本 | 提取到 `BaseAgent` 基类 | ⬜ 待处理 | 重构项 |
| Q-4.7 | `LLMFactory.stats` 的 `records` 列表无并发保护 | 多线程下统计可能不准 | 加 `threading.Lock` 或改用 `collections.deque` + 原子操作 | ⬜ 待处理 | asyncio 单线程下无影响 |
| Q-4.8 | `get_config` 使用 `lru_cache` 但参数含路径，不同路径不会命中缓存 | 多配置文件场景 | 确认是否需要支持多配置；若不需要可移除路径参数 | ⬜ 待处理 | 当前单配置无影响 |
| Q-4.10 | `requirements.txt` 可能含未使用的依赖 | 镜像体积 | 用 `pip-audit` 或 `pip-tools` 检查未使用包 | ⬜ 待处理 | 代码卫生 |
| Q-4.11 | `now_iso()` 在 `storage/base.py` 和 `graph/state.py` 重复定义 | 维护成本 | 提取到 `core/utils.py` | ⬜ 待处理 | 重构项 |
| Q-4.12 | `builder.py` 的条件路由函数签名与 LangGraph 1.x 不完全匹配 | 升级风险 | 升级 LangGraph 时需验证 `should_continue` 签名 | ⬜ 待处理 | 当前版本可用 |
| Q-4.13 | 测试覆盖不足：5 个核心 Agent / 工具层 / API 路由 / CLI 均无单元测试 | 回归风险 | 补充对应单元测试 | 🔄 进行中 | 本轮已补 P0 修复测试；Agent/API 测试待补 |
| NEW-D | API/CLI 从存储恢复历史到短期记忆时连续 `add_message` 未触发 `compress_if_needed`，超大历史会使内存超限 | 内存占用；大历史会话恢复 | 恢复完成后调用一次 `compress_if_needed` | ⬜ 待处理 | 改动小；滑动窗口已限制工作集，影响有限 |
| NEW-E | `registry.py` 中 `except (MCPError, Exception)` 冗余（MCPError 是 Exception 子类） | 代码可读性 | 简化为 `except Exception` | ⬜ 待处理 | 无害，纯代码卫生 |
| NEW-G | CLI `/stats` 命令未展示 `retry_rate`/`degradation_rate`（属性已在 `LLMCallStats` 实现） | 质量监控可见性 | `/stats` 表格增加重试率/降级率行 | ⬜ 待处理 | 改动小 |

### 🟡 Phase 2 Code Review 暂缓问题（2026-08-17）

> 来源：Qoder + KnowledgeBase 两份 Phase 2 审查报告，以下问题经评估可暂缓修复。

| 编号 | 严重度 | 问题摘要 | 影响范围 | 建议处理方式 | 状态 | 备注 |
|------|--------|----------|----------|--------------|------|------|
| P2-P2-01 | 🟡 P2 | 文件上传一次性读入内存（`await file.read()`），50MB 文件先占满内存 | 生产 DoS 风险 | 流式读取 + Content-Length 预校验；PDF/docx 畸形文件防护 | ⏸️ 生产前 | Phase 2 开发阶段单用户可接受 |
| P2-P2-02 | 🟡 P2 | 文件入库逐块 `add`，未使用 `add_batch` 批量入库 | 性能（大文件入库慢） | `DirectVectorStore` 增加 `add_batch`；`_ingest_chunks` 改用批量接口 | ⬜ 待处理 | 功能正确，性能优化项 |
| P2-P2-03 | 🟡 P2 | RAG 上下文格式化逻辑重复 3 处（retriever/executor/dispatch_tool） | 代码维护 | 统一为 `format_rag_context` 一处实现，executor 复用 | ⬜ 待处理 | 格式略有差异，需先设计统一规范 |
| P2-P2-04 | 🟡 P2 | Embedding 模型加载失败后永久降级为哈希向量，无恢复机制 | 生产检索质量崩塌 | 区分"未安装"与"加载失败"；加载失败支持重试；加 `threading.Lock` | ⏸️ 生产前 | 开发环境便利设计；生产前需加 strict 模式 |
| P2-P2-05 | 🟡 P2 | `tools.vector_store.enabled` 是死配置，bootstrap 只检查 `l3_knowledge.enabled` | 配置误导 | 删除 `tools.vector_store.enabled` 或 bootstrap 同时检查两者 | ⬜ 待处理 | 不影响功能，配置清理项 |
| P2-P2-06 | 🟡 P2 | `count(user_id)` 用 `collection.get` 加载全部条目后 `len()` | 万级条目时 `/status` 接口慢 | 用 `include=[]` 只取 ids；或维护计数缓存 | ⬜ 待处理 | 当前数据量小无影响 |
| P2-P2-07 | 🟡 P2 | `from_chroma_record` 的 `metadata` 参数名与类字段同名（shadowing） | 代码可读性 | 参数改名 `raw_meta` 或 `chroma_meta` | ⬜ 待处理 | 不影响正确性 |
| P2-P2-08 | 🟡 P2 | 后台任务 `_background_ingest` 持有 `AppContext`，`/shutdown` 后可能访问已释放资源 | 后台任务异常 | 改用闭包捕获所需依赖；或独立队列/worker | ⏸️ 生产前 | Phase 2 无 `/shutdown` 场景 |
| P2-P2-09 | 🟡 P2 | `upload.py` 异常处理将内部错误细节返回客户端（信息泄露） | 安全 | 统一错误响应规范，内部错误返回通用提示 | ⏸️ Phase 3 | 安全加固项 |
| P2-P2-10 | 🟡 P2 | 冲突检测阈值（0.95/0.80）未用真实 embedding 验证 | 可能误合并/误并存 | 用 bge-small-zh 对"同义/补充/无关"三类文本实测 distance 分布校准 | ⏸️ 生产前 | 需安装 sentence-transformers 后验证 |
| P2-P2-11 | 🟡 P2 | `KnowledgeBaseBackend` 抽象接口与实现漂移：`retrieve` 缺 `min_score`；`find_similar` 未声明 | 接口契约不一致 | 接口增加 `min_score`/`find_similar`；消除 `getattr` hack | ⬜ 待处理 | 中期重构项 |
| P2-P2-12 | 🟡 P2 | `knowledge_ingestor.py` 的 `user_input`/`answer` 直接插入 Prompt，存在 prompt injection 风险 | 知识库纯净度 | 对用户输入做清洗/转义 | ⏸️ Phase 3 | 低概率但影响知识质量 |
| P2-P2-13 | 🟡 P2 | `rag_retrieval_node` 每次请求新建 `RAGRetriever`（无状态对象），重复解析配置 | 微小性能浪费 | 在 `GraphBuilder.__init__` 中缓存实例 | ⬜ 待处理 | 改动小 |
| P2-P2-14 | 🟡 P2 | `upload.py` 异步处理占位响应 `status="success"` 但 `error="后台异步处理中"`，语义矛盾 | 接口语义混淆 | status 改为 `"processing"` 或 `"accepted"` | ⬜ 待处理 | 改动小 |
| P2-P2-15 | 🟡 P2 | 事实提取/冲突检测失败直接丢弃知识，无重试/补偿 | 有价值知识可能丢失 | 记录 conv_id 到待重试队列；或离线补采 | ⏸️ Phase 3 | LLM 内部已有 tenacity 重试 |

---

## 三、变更记录

| 日期 | 变更内容 |
|------|----------|
| 2026-08-13 | 初始建立；记录 6 个 P0 已修复 + 6 个附加修复 + 14 个未修复问题 |
| 2026-08-13 | 全量 review 补充：新增修复 NEW-A/NEW-B/NEW-F（+5 测试，共 35 项）；新增未修复 NEW-D/NEW-E/NEW-G；全量测试 239 passed |
| 2026-08-13 | 误操作恢复：全部源码修复与测试文件被撤销后完整重建；全量测试验证通过 |
| 2026-08-17 | Phase 2 新增修复 P0-1~P0-6 + P1-7~P1-11（共 11 项）；新增 Phase 2 暂缓问题 15 项；全量测试 401 passed |

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 端侧推理与端云协同都要联网：
         edgeBaseUrl 指向宿主机/本机的 OpenAI 兼容端点，sekbBaseUrl 指向云端。
         明文 HTTP 只对 10.0.2.2（模拟器里的宿主机）放开——见 network_security_config。 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 第三个设备工具要读联系人：**故意**申请一个危险权限，用来验证"越权拦截"
         （未授权时工具调用必须被拦下并留审计，见 tools/PermissionAudit.kt） -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".SekbApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.SekbOnDevice">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

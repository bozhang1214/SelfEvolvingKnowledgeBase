以下是RAG系统上线实际使用中常见的20个问题及对应的解决建议，按问题类型分组整理：

***

## 一、文档解析与编码问题

### 问题1：PDF中文内容全部显示为乱码

**现象**：上传的中文PDF文档经过解析后，提取出的文本全是乱码或不可读字符。

**原因**：`pypdf`等库默认使用`latin-1`解码，遇到中文PDF直接解析失败；或PDF文件中文字体未嵌入，导致提取时字符映射错误。

**解决方案**：

- 解析时强制指定UTF-8编码
- 将`pypdf`替换为`pdfplumber`或`pymupdf`，对中文支持更好
- 设置环境变量`LANG=zh_CN.UTF-8`后再启动服务
- 若已创建知识库后才设置编码，需重建向量库

### 问题2：扫描版PDF无法提取任何文字

**现象**：上传的PDF是图片扫描件，解析后返回空内容或极少文字。

**原因**：扫描版PDF不含文本层，常规PDF解析器无法读取。

**解决方案**：

- 引入OCR能力作为兜底方案
- 使用多层OCR策略：EasyOCR → PaddleOCR → pytesseract 逐级降级
- 对扫描件进行预处理：检测多栏布局、将DPI标准化至300以上
- 在文档上传与模型处理之间增加“通用文档解析层”

### 问题3：繁体中文文档检索效果极差

**现象**：知识库中包含繁体中文文档，检索时召回率远低于简体中文文档。

**原因**：Embedding模型主要在简体中文语料上训练，对繁体字的语义表征能力不足；繁体与简体的字形差异导致向量空间距离被拉大。

**解决方案**：

- 入库前将繁体统一转换为简体（使用OpenCC等工具）
- 选择对繁体有专门优化的Embedding模型
- 在查询时也做繁简转换，保持query与doc的字符集一致
- 对繁体文档使用MLLM进行后OCR文本增强

### 问题4：加密PDF无法解析

**现象**：上传带密码保护的PDF时解析失败或报错。

**原因**：PDF文件设置了打开密码或权限密码。

**解决方案**：

- 在文档上传流程中增加密码输入字段
- 使用`pymupdf`等支持密码参数的库进行解密后解析
- 若无法获取密码，建立人工处理队列

### 问题5：Word文档中的公式和特殊符号丢失

**现象**：从Word文档提取的文本中，数学公式、特殊符号变成空白或乱码。

**原因**：`mammoth`等Word解析库主要提取文本内容，对OLE对象、公式对象支持有限。

**解决方案**：

- 使用`python-docx`配合`mathml`解析公式
- 将公式渲染为LaTeX格式后再存储
- 对包含大量公式的文档，考虑使用专业的文档解析服务

## 二、文本分块（Chunking）问题

### 问题6：按固定长度切分导致语义被切断

**现象**：检索到的文本块不完整，关键信息被切成两半。

**原因**：按固定字符数或Token数直接切分，不考虑句子边界和段落结构。

**解决方案**：

- 使用递归分块（Recursive Chunking），优先按段落、句子边界切分
- 设置10%-20%的重叠率（Overlap），保证边界信息不丢失
- 推荐chunk大小300-800字或300-500 tokens
- 先按句子切分，再聚合成满足大小的块

### 问题7：小块丢失上下文，大块引入噪声

**现象**：100-200 Token的小块检索精准但缺乏上下文；1000+ Token的大块包含完整上下文但检索不准。

**原因**：小块切碎语义，代词“它”“这个项目”失去指代对象；大块将整页压缩进向量，细节特征被稀释。

**解决方案**：

- 采用Small-to-Big（由小到大）检索策略：用小块检索，用母块（Parent Document）提供上下文
- 不同文档类型使用不同分块策略
- 从512 tokens + 10%-15%重叠率开始调试

### 问题8：表格被拍扁后信息完全丢失

**现象**：用户问“iPhone 14的电池容量是多少”，RAG把iPhone 13的电池容量张冠李戴。

**原因**：传统RAG将表格按行或列展平成纯文本，摧毁了表格的行列表头交叉映射关系。

**解决方案**：

- 解析时保留表格结构：表头层级、数据行归属、合并单元格关系
- 表头信息随数据行一起存储
- 跨页表格按文档级对象处理
- 使用Docling等结构保持型解析工具

## 三、检索与召回问题

### 问题9：明明文档里有答案，但模型就是搜不到

**现象**：知识库中明确包含答案，但RAG检索不到，模型回答“未找到相关信息”。

**原因**：文档切分策略错误导致语义被破坏；Embedding模型不适合业务语料；查询语句和文档语义“不在一个空间”；只做向量召回没有关键词兜底。

**解决方案**：

- 换用更适合业务场景的Embedding模型（如中文场景用BGE-large-zh）
- 增加混合检索：向量检索 + BM25关键词检索
- 加入Reranker对召回结果重新排序
- 在检索前做查询改写，将口语化问题转为检索友好格式

### 问题10：检索相似度高，但答案完全错误（静默故障）

**现象**：余弦相似度分数很高，但LLM给出的答案与事实严重偏差。

**原因**：余弦相似度衡量的是向量距离，不是“能否回答问题”。检索到的文本块主题相关但不包含答案所需信息，LLM用外推填补空白。

**解决方案**：

- 在检索与生成之间增加“相关性门控”（Relevance Gate），验证检索块是否真正能回答问题
- 生成后增加自评估层，检测答案是否有事实支撑
- 建立反馈循环，收集用户的重问、负面评价等信号

### 问题11：多轮对话中后续追问无法理解指代

**现象**：用户先问“Python装饰器是什么”，再问“它有哪些应用场景”，RAG无法理解“它”指代Python装饰器。

**原因**：多轮对话中，后续查询（如“具体数据是多少？”“它怎么用？”）没有利用对话历史来补全上下文。

**解决方案**：

- 在检索前进行查询改写（Query Rewrite），将指代消解、省略补全
- 将对话历史作为上下文注入检索流程
- 使用滑动窗口管理对话历史，保留最近N轮

### 问题12：向量检索无法处理数值范围和逻辑过滤

**现象**：用户问“找出2023年以后股价>50 USD的公司年报”，向量检索无法做范围过滤。

**原因**：向量检索基于语义相似度，不具备结构化查询能力。

**解决方案**：

- 增加元数据（Metadata）过滤，在检索前按时间、分类等缩小范围
- 使用混合流水线：显性路由逻辑，结构化查询走SQL，语义查询走向量
- 后置SQL过滤，召回后做二次筛选

## 四、性能与延迟问题

### 问题13：文档一多检索就变得极慢

**现象**：知识库从几百份文档扩展到几万份后，检索延迟从几百毫秒飙升至数秒。

**原因**：向量数量膨胀（10万文档→50万\~300万chunk）；使用Flat索引；Embedding维度过高；查询阶段全量扫描。

**解决方案**：

- 使用HNSW、IVF等高效索引替代Flat索引
- 对向量库进行分片或分区
- 使用索引压缩技术
- 考虑流水线化执行：将检索与LLM预填充并行

### 问题14：首个Token生成延迟随上下文增长呈二次方增长

**现象**：随着对话轮次增加，响应越来越慢。

**原因**：上下文窗口不断增长，首个Token的生成延迟呈二次方增长。

**解决方案**：

- 使用上下文压缩层，减少KV Cache占用30-45%
- 对历史对话做摘要压缩而非全量保留
- 使用轻量级编码器生成压缩的块嵌入

### 问题15：向量库索引重建耗时数小时

**现象**：每次知识库更新都需要全量重建索引，导致服务中断数小时。

**原因**：采用全量重建策略，而非增量更新。

**解决方案**：

- 实施增量索引更新（Incremental Indexing）
- 将全量重造改为增量修补，将P99延迟影响从210%压缩至约12%
- 避免“存储”与“索引”分离导致的数据流断裂

## 五、部署与运维问题

### 问题16：RAG系统各组件耦合严重，维护困难

**现象**：重建一次向量库，检索逻辑、相似度阈值、重排策略等三四处地方都要改。

**原因**：Embedding服务、向量数据库、文档存储、Rerank、LLM服务等各层“随便拼的”，后期无法维护。

**解决方案**：

- 采用系统思维而非模型思维：20%模型 + 80%系统
- 建立清晰的分层架构和接口契约
- 使用统一的可观测性与评估架构，以Session ID贯穿全链路追踪

### 问题17：线上环境问题伪装成“模型不行”

**现象**：开发环境一切正常，上线后频繁超时或报错。

**原因**：网络代理配置、环境变量作用域（Preview vs Production）、Serverless出站网络等问题。

**解决方案**：

- 建立完整的监控体系：Prometheus + Grafana监控检索/嵌入/生成各环节延迟
- 定义关键性能指标：准确性、响应时间、资源利用率
- 配置预测性告警机制

### 问题18：知识更新后旧数据仍被检索到

**现象**：文档已更新，但RAG仍然返回旧版本的内容。

**原因**：增量同步延迟，新文档没进索引；旧片段被标记为“过期”但与新语义权重冲突。

**解决方案**：

- 建立实时或准实时的索引更新机制
- 实施版本化存储，检索时按时间戳过滤
- 定期清理过期向量

## 六、安全与评估问题

### 问题19：用户A检索到了用户B的文档内容

**现象**：多租户场景下，用户查询返回了其他用户的私有文档。

**原因**：权限过滤在检索之后进行（Post-filter），无权限数据已被暴露给检索层。

**解决方案**：

- 权限过滤前置（Pre-filter）：在向量检索时直接应用文档级权限
- 使用RAGGuard等安全层，在向量数据库查询中强制执行文档级权限
- 与现有授权系统（OPA、Cerbos、OpenFGA等）集成

### 问题20：无法评估RAG系统质量，优化全靠感觉

**现象**：系统上线后不知道好坏，优化方向全凭直觉。

**原因**：缺乏系统化的评估指标和自动化测试体系。

**解决方案**：

- 建立多维评估框架：引用质量（Citation Quality）、延迟与成本（Latency and Cost）等
- 使用LLM作为评估器，自动化评估Faithfulness（忠实度）等指标
- 建立黄金测试集（Golden Q/A Set），每次迭代后自动运行回归测试
- 可参考CLEAR-RAG等生产级RAG评估框架

## 总结

以上20个问题覆盖了RAG系统从**文档输入**（编码、解析、分块）到**检索召回**（相似度、多轮对话、结构化查询）再到**性能部署**（延迟、索引、运维）和**安全评估**（权限、可观测性）的全链路。解决这些问题的核心思路可归纳为：

1. **数据质量是最大瓶颈**：文档预处理的重要性远超模型选择
2. **混合检索是性价比最高的优化**：向量+BM25，改几行代码涨5个点召回率
3. **可观测性是生产环境的生命线**：静默故障比显式报错更可怕
4. **安全必须前置**：不要让模型看到无权数据是安全底线

